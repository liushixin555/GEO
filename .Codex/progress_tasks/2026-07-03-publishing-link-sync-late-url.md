# 2026-07-03 软盟发布链接迟到同步修复

- 根因：订单同步只扫描 `rm_status = 0`，软盟订单变为非 0 状态后若最终公开 URL 迟到返回，系统不会再次查询。
- 修复：近 7 天内没有公开发布链接的订单继续轮询；响应体任意字段出现非 `ruan.net` 公开 URL 即写入 `published_article_links`；同步时清理 `i.ruan.net/manuscripts` 等内部稿件链接。
- 本地执行一次同步后，7 个订单成功同步，6 条真实公开 URL 入库，1 条红商网仍未返回 URL，保留待补并继续轮询。
