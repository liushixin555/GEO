# dev003. 技能管理

> 状态：✅ 已完成

---

## 功能说明

管理系统中的技能库。技能不属于任何公司和项目，是全局共享资源。通过上传 zip 包（含 SKILL.md）创建技能，系统自动从 SKILL.md 的 frontmatter 中提取名称和描述。

### 技能列表

- 地址：`/skills`
- 系统管理员和运营者可访问
- 双视图响应式展示：>=1280px 显示表格视图（antd Table），<1280px 显示卡片视图（antd Card）
- 表格列：技能名称、描述、添加者、添加时间、操作（删除）
- 卡片视图：名称为卡片标题，描述独占一行（最多5行省略），添加者和添加时间显示在底部元数据行
- 支持搜索（按技能名称）
- 分页展示

### 技能上传（创建）

- 点击「添加技能」按钮弹出上传 Modal
- 上传包含 SKILL.md 的 zip 包
- 支持格式：`.zip`
- 后端解析流程：
  1. 保存 zip 到临时目录
  2. 解析 SKILL.md 获取技能名称和描述（不提取文件）
  3. 查询数据库是否存在同名软删除记录
  4. 解压到 `skills/{目录名}/`（软删除同名存在时允许覆盖残留目录）
  5. 校验 SKILL.md 存在且 frontmatter 格式正确（必须有 `name` 字段）
  6. **同名规则铁律**：
     - 如果存在**未删除**的同名技能 → 报错「已存在同名技能」，上传失败
     - 如果存在**已删除**的同名技能 → 上传成功，用新数据刷新数据库记录（description、skillDir、createdBy），并将 deletedAt 设为 null
     - 如果不存在同名技能 → 创建新记录
  7. 写入数据库记录

### SKILL.md 格式要求

```markdown
---
name: 技能名称
description: 技能描述
---

# 技能标题

技能正文内容...
```

### API 端点

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/skills` | sysadmin + admin | 获取技能列表（分页、搜索） |
| GET | `/api/skills/:id` | sysadmin + admin | 获取技能详情 |
| POST | `/api/skills` | sysadmin + admin | 上传 zip 包创建技能（multipart/form-data） |
| PUT | `/api/skills/:id` | sysadmin + 创建者 | 更新技能 |
| DELETE | `/api/skills/:id` | sysadmin + 创建者 | 删除技能（同时删除技能目录） |

---

## 业务规则

1. **技能不属于任何公司和项目** — 技能是全局资源，无公司归属
2. **系统管理员（sysadmin）**：可以查看、上传、删除所有技能，无限制
3. **运营者（admin）**：
   - 可以查看所有技能
   - 可以上传新技能，上传时系统自动记录创建者
   - 只能删除**自己上传的技能**
   - 尝试删除他人技能时返回 403 错误
4. 技能名称唯一，不允许重复（活跃记录）
5. 删除技能时同时删除 `skills/` 下对应的目录（软删除数据库记录 + 硬删除文件）
6. 已删除的同名技能重新上传时，复用原数据库记录刷新数据并恢复为未删除状态
7. 无禁用/启用功能

---

## 验收标准

- [x] 系统管理员可以查看、上传、删除所有技能
- [x] 运营者可以查看所有技能
- [x] 运营者可以上传新技能
- [x] 运营者只能删除自己上传的技能
- [x] 运营者删除他人技能时返回 403
- [x] 可以按名称搜索
- [x] 上传 zip 包时自动解析 SKILL.md 提取名称和描述
- [x] SKILL.md 格式不正确时报错
- [x] 同名技能不允许重复创建
- [x] 已删除同名技能重新上传时复用记录并恢复
- [x] 删除时同时清理文件目录

---

## 评审修复记录

### 2026-05-26 skills.entity.ts 四维评审修复（4.0→7.5/10）

**评审文件**: `tasks/review/skills.entity.ts.{committer|security|architecture|quality}.md`

**修复项**:

| 编号 | 级别 | 问题 | 修复方案 | 影响文件 |
|------|------|------|---------|---------|
| B-1 | BLOCKING | `deleted_at` 遗漏 | `Skills` 添加 `deleted_at: Date \| null`，`mapSkills` 添加映射 | entity, map |
| B-2 | BLOCKING | `created_by` 身份伪造 | 从 `CreateSkillsRequest` 移除，service.create 接受独立参数 | entity, service, controller |
| H-1 | HIGH | `creator_name` 混入基础实体 | 拆分 `Skills`/`SkillsDetail extends Skills` | entity, index, map, service |
| H-2 | HIGH | `description` 不可清除 | `UpdateSkillsRequest.description` 改为 `string \| null` 三态 | entity |
| H-3 | HIGH | 零 JSDoc 文档 | 补全全部字段 JSDoc + 模块级注释 | entity |
| H-4 | HIGH | `skill_dir` 路径泄露 | JSDoc 标注服务端内部使用 | entity |

**修改文件清单**:
- `apis/entity/skills.entity.ts` — 核心实体重构
- `apis/entity/index.ts` — 导出 SkillsDetail
- `apis/map/index.ts` — mapSkills 返回 SkillsDetail + deleted_at 映射
- `apis/service/skills.service.ts` — create 签名拆分 createdBy 参数
- `apis/service/impl/skills.service.impl.ts` — 使用 createdBy 参数
- `apis/controller/skills.controller.ts` — create 调用参数拆分
- `tests/apis/skills.entity.test.ts` — 适配新接口，13 测试全通过
