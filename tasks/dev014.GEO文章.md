# dev014. GEO文章

> 状态：✅ 已完成（安全加固 + Entity 类型修复）

---

## 功能说明

管理项目下的GEO文章。每篇文章归属于一个项目，具有完整的生命周期状态流转（草稿→AI生成→审核→发布），支持AI知识库关键词/画像/图片关联，以及多平台发布。

### 文章列表

- 地址：`/project/:projectId/articles`
- 系统管理员和运营者可访问，运营者需有当前项目的权限
- 以卡片网格形式展示文章
- 双视图响应式展示：>=1280px 显示表格视图（antd Table），<1280px 显示卡片视图（antd Card + Descriptions）
- 表格视图和卡片视图显示完全一致的信息：标题、关键词、状态、创建时间、操作
- 支持搜索（按文章标题）、按状态筛选
- 分页展示

### 文章表单（新建/编辑）

独立路由页面形式（非弹窗），包含以下字段：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 文章标题 | 文本输入 | 手工必填，AI模式可选 | 文章标题（AI模式下由AI生成） |
| 文章关键词 | 多选 | 否 | 只能从知识库关键词中选择 |
| 文章画像 | 多选 | 否 | 只能从知识库画像中选择（多选） |
| 文章插图 | 多选 | 否 | 只能从知识库图片中选择（网格多选） |
| 发布平台 | 多选下拉 | 否 | 只能选择，选择源来自发布平台数据表 |

### 文章详情/编辑页面

- 独立路由页面（`/article/:id`），非弹窗
- 包含 Tab 标签页切换：「文章设置」和「正文」
- **文章设置 Tab**：展示文章标题、关键词、画像、插图、发布平台等字段，可编辑状态下可修改
- **正文 Tab**：
  - 仅在文章 content 不为 null 且不为空时显示
  - 展示当前版本号（如 v1.0、v2.0）
  - 支持正文内容编辑和保存
  - 每次保存正文，自动递增版本号（1.0 → 2.0 → 3.0 …），并保存版本历史
  - 在文章处于**待审核**状态时，文章创建者仍可手工编辑正文
- 非可编辑状态时，表单字段为只读（disabled）

### API 端点

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/projects/:projectId/articles` | sysadmin + admin（需项目权限） | 获取项目下的文章列表 |
| GET | `/api/projects/:projectId/articles/:id` | sysadmin + admin（需项目权限） | 获取文章详情 |
| POST | `/api/projects/:projectId/articles` | sysadmin + admin（需项目权限） | 创建文章 |
| PUT | `/api/projects/:projectId/articles/:id` | sysadmin + 创建者 | 更新文章 |
| DELETE | `/api/projects/:projectId/articles/:id` | sysadmin + 创建者（仅草稿状态） | 删除文章 |
| PUT | `/api/projects/:projectId/articles/:id/review` | sysadmin + admin（需项目权限） | 审核文章（通过/不通过） |
| PUT | `/api/projects/:projectId/articles/:id/content` | sysadmin + 创建者 | 更新文章正文（支持待审核状态编辑） |
| GET | `/api/projects/:projectId/articles/:id/versions` | sysadmin + admin（需项目权限） | 获取文章正文版本历史 |

---

## 状态流转

```
草稿 ──(保存并提交)──→ AI生成中
AI生成中 ──(成功)──→ 待审核
AI生成中 ──(失败)──→ AI生成失败
AI生成失败 ──(编辑后提交)──→ AI生成中
待审核 ──(审核通过)──→ 发布中
待审核 ──(审核不通过)──→ 草稿
发布中 ──(成功)──→ 发布成功
发布中 ──(失败)──→ 发布失败
发布失败 ──(编辑后提交)──→ AI生成中
```

各状态下的用户操作权限：

| 状态 | 查看 | 编辑 | 其他操作 |
|------|------|------|----------|
| 草稿 | 有权限用户 | 创建者/sysadmin | 可删除 |
| AI生成中 | 有权限用户 | — | — |
| AI生成失败 | 有权限用户 | 创建者/sysadmin | 编辑后重新提交，回到AI生成中 |
| 待审核 | 有权限用户 | 创建者可编辑正文 | 审核通过→发布中；审核不通过→草稿 |
| 发布中 | 有权限用户 | — | — |
| 发布失败 | 有权限用户 | 创建者/sysadmin | 编辑后重新提交，回到AI生成中 |
| 发布成功 | 有权限用户 | — | — |

---

## 业务规则

1. **权限隔离**：
   - 系统管理员可查看和操作所有项目的文章
   - 运营者只能查看和操作自己有权限的项目下的文章
   - 运营者访问无权限的项目文章时返回 403 错误
2. **编辑权限**：
   - 仅文章创建者和sysadmin角色可编辑文章
   - 编辑仅在草稿、AI生成失败、发布失败状态下可用
   - 编辑后提交，文章回到AI生成中状态
3. **查看权限**：
   - sysadmin可查看所有文章
   - 当前项目的所有运营者（有项目权限的admin用户）可查看该项目下的文章
4. **删除限制**：
   - 仅草稿状态下可删除文章
   - 仅文章创建者和sysadmin可删除
5. **审核操作**：
   - 待审核状态的文章，有项目权限的用户可进行审核
   - 审核通过→进入发布中
   - 审核不通过→回到草稿状态
   - **创建者不能审核自己的文章**（职责分离 SoD 原则，sysadmin 除外）
6. **创建者记录**：创建文章时系统自动记录创建者身份
7. **文章标题**为必填项
8. **文章正文版本管理**：
   - 文章 content 字段存储当前正文内容
   - 每次保存正文，版本号自动递增（1.0 → 2.0 → 3.0 …）
   - 每个版本的历史内容保存在 article_versions 表中
   - 版本号规则：整数递增，显示为 x.0 格式
9. **待审核状态正文编辑**：
   - 文章处于待审核（pending_review）状态时，创建者仍可编辑正文内容
   - 通过专用 API 端点 `PUT .../content` 更新，不影响文章设置字段的编辑权限
10. **文章页面为独立路由**：
    - 列表页：`/article`
    - 新建/编辑/查看：`/article/:id`（id 为 `new` 或数字）
    - 使用 Tab 标签页切换「文章设置」和「正文」
    - 非编辑状态时表单字段为只读
11. **手工编写模式文档导入**：
    - 选择「手工编写」后，标题 Form.Item 的 label 旁显示「导入」按钮
    - 支持 `.md`、`.doc`、`.docx` 格式
    - 解析逻辑：
      - `.md`：直接读取文本，提取 `# 一级标题` 作为标题，剩余内容作为正文
      - `.doc/.docx`：通过 mammoth 转 HTML，再将 h1/h2/h3/p 转为 markdown 格式
    - 填充规则：
      - 有 `# 一级标题` → 自动填充文章标题，并从正文中移除该标题
      - 无一级标题 → 不填充标题（不报错），仅填充正文
    - 正文始终自动填充
12. **文章标题显示**：标题为 null 或空时，列表和详情显示"待生成"

---

## 验收标准

- [ ] 系统管理员可查看所有项目的文章
- [ ] 运营者只能查看自己有权限的项目下的文章
- [ ] 运营者访问无权限项目的文章时返回 403
- [ ] 只有创建者和sysadmin可编辑文章
- [ ] 编辑仅限草稿/AI生成失败/发布失败状态
- [ ] 编辑提交后文章回到AI生成中状态
- [ ] 仅草稿状态下可删除文章
- [ ] 待审核状态可审核通过/不通过
- [ ] 审核通过进入发布中，不通过回到草稿
- [ ] 关键词只能从知识库关键词中选择
- [ ] 画像只能从知识库画像中选择（多选）
- [ ] 插图只能从知识库图片中选择（网格多选）
- [ ] 发布平台从发布平台数据表中选择
- [ ] 必填字段为空时提示错误
- [ ] 新建/编辑/查看为独立路由页面而非弹窗
- [ ] 文章有正文时Tab切换显示「文章设置」和「正文」
- [ ] 正文每次保存自动递增版本号（1.0 → 2.0 → …）
- [ ] 版本历史保存在 article_versions 表
- [ ] 待审核状态创建者仍可编辑正文
- [ ] 非编辑状态时字段为只读（disabled）

---

## 安全加固记录（2026-05-23）

根据代码安全专家评审 + Committer 审核意见，修复以下安全漏洞：

| 编号 | 级别 | 修复内容 |
|------|------|----------|
| CRITICAL-1 | CRITICAL→HIGH | `updateArticle`/`createArticle` 添加字段白名单过滤，阻止任意字段注入 |
| CRITICAL-2 | CRITICAL | 引入状态转换白名单 `STATUS_TRANSITIONS`，阻止绕过审核流程 |
| HIGH-1 | HIGH→MEDIUM | `createArticle` 白名单验证 |
| HIGH-2 | HIGH | `reviewArticle` 添加创建者/审核者职责分离检查 |
| HIGH-3 | HIGH | 统一错误处理 `handleServerError`，500 错误不返回 `err.message` |
| HIGH-4 | HIGH | `updateArticleContent` 添加 `MAX_CONTENT_LENGTH=500KB` 限制 |
| MEDIUM-2 | MEDIUM | 分页参数添加上下限（page≥1, 1≤pageSize≤100） |
| MEDIUM-3 | MEDIUM | `regenerateArticle` 添加创建者所有权检查 |
| MEDIUM-4 | MEDIUM | `req.user!` 替换为防御性 `getAuthUser()` 检查 |
| MEDIUM-5 | MEDIUM | `checkProjectOperator` 区分权限拒绝（`PermissionDeniedError`）和系统错误 |
| LOW-1 | LOW | `search` 参数清理（trim + 200字符截断） |
| LOW-4 | LOW | `parseInt` 统一指定基数 10 |
| 补充-1 | — | `generating` 分支排除 `content` 字段，避免版本记录污染 |

---

## 软件质量评审修复记录（2026-05-24）

根据软件质量专家评审（tasks/review/ArticleDetail.tsx.md），修复以下问题：

### 已修复项（本次）

| 编号 | 严重度 | 修复内容 |
|------|--------|----------|
| Q-09 | 🟡 MEDIUM | 提取 `submitForm` 公共函数，消除 3 处重复的 form.validateFields 代码 |
| Q-11 | 🟢 LOW | 修复 CSS 变量引用：`--text-secondary` → `--color-ink-subtle`、`--border-subtle` → `--color-hairline`、`--interactive` → `--color-primary` |
| Q-10 | 🟡 MEDIUM | 消除 `any` 类型：新增 SkillApiItem/LlmModelApiItem/KbKeywordApiItem/KbPortraitApiItem/KbImageApiItem 接口 |
| Q-08 | 🟡 MEDIUM | 统一错误处理：静默 catch 块改为 `console.warn`，关键操作 `message.error` |
| Q-06 | 🟡 MEDIUM | 知识库 API 添加模块级缓存（kbCache + optionsCache），避免重复请求 |

### 已在先前迭代中修复项

| 编号 | 修复内容 |
|------|----------|
| Q-01 | 组件拆分：889 行 → 202 行主组件 + 6 子组件 + 6 hooks |
| Q-02/SEC-01 | mammoth XSS 风险：使用 DOMPurify 清理 HTML |
| Q-03/SEC-02 | localStorage 解析：添加 try-catch 容错 |
| Q-04 | Token DRY：axios 实例 + 请求拦截器统一注入 |
| Q-05 | useEffect 依赖项补全 |
| Q-07 | 条件渲染扁平化（拆分子组件） |
| Q-12 | Spin 添加 tip 文字提示 |
| Q-13 | 移除 Collapse forceRender |

### 新增测试

- `tests/pages/article/hooks/useKnowledgeBase.test.ts`：6 个测试用例

---

## Entity 类型安全评审修复记录（2026-05-25）

根据代码安全专家评审 + Committer 审核意见（`tasks/review/article.entity.md`），修复以下类型安全问题：

### 先前迭代已修复项

| 编号 | 级别 | 修复内容 |
|------|------|----------|
| CRITICAL-2 | CRITICAL | `UpdateArticleRequest.status` 从 `string` 改为 `ArticleStatus` 联合类型 |
| 遗漏-1 | HIGH | `Article.skills` 从 `number \| null` 改为 `unknown \| null`，对齐 Prisma `Json?` |
| HIGH | HIGH | 创建 `article.schema.ts` Zod Schema，集成到路由 validate 中间件 |
| HIGH-1 | MEDIUM/HIGH | 所有字符串字段添加 Zod 长度约束（title≤500, keywords≤500, portrait≤2000 等） |
| HIGH-2 | MEDIUM | `content` 字段添加 JSDoc 安全声明：`文章正文（纯文本，禁止 HTML）` |
| HIGH-3 | MEDIUM | `scheduled_publish_at` 添加 `z.string().datetime({ offset: true })` 格式校验 |
| MEDIUM-1 | MEDIUM | `images`/`platforms` 数组添加元素数（max 20/10）和元素长度约束 |
| LOW-1 | LOW | `llm_model_id` 使用 `z.number().int().nonnegative()` |

### 本次迭代修复项

| 编号 | 级别 | 修复内容 |
|------|------|----------|
| 类型一致性 | HIGH | `UpdateArticleRequest.skills` 从 `unknown` 改为 `number[] \| null`，对齐 Zod Schema 和 CreateArticleRequest |
| 类型完整性 | MEDIUM | `CreateArticleRequest.skills` 从 `number[]` 改为 `number[] \| null`，对齐 Zod `.nullable()` |
| 测试对齐 | — | 测试用例更新：`skills: 2` → `skills: [2]`，新增 `skills: null` 测试 |

### 第三轮 Committer 评审修复记录（2026-05-26）

根据 Committer 审核专家评审（tasks/review/article.entity.fix.md），修复以下问题：

| 编号 | 级别 | 修复内容 |
|------|------|----------|
| M-1 | MEDIUM | 测试 `baseArticle.skills: 1` → `[1]`，反映真实 API 数据流 |
| M-2 | MEDIUM | 测试 `skills: 0` → `[0]`，对齐实际数据结构 |
| L-1 | LOW | 修复死断言：`approve.approve` → `approve.approved` |
| L-2 | LOW | 测试 article_type 更新为前端定义值（'案例分析' 等） |
| L-3 | LOW | Entity 补充 JSDoc 文档（article_type 推荐值、write_mode 推荐值、skills API 说明） |

### BACKLOG（未修复，需后续迭代）

| 编号 | 级别 | 说明 |
|------|------|------|
| MEDIUM-3 | BACKLOG | `ReviewArticleRequest` 添加 `reason?: string` 字段（需 DB 迁移） |
| 遗漏-2 | LOW | `version` 从 Prisma `Float` 改为 `Int`（需 DB 迁移） |

### 第四轮五维评审修复记录（2026-05-26）

根据安全评审（3.2/10）+ 架构评审（2.9/10）+ 质量评审（4.0/10）+ UI评审（3.8/10）+ Committer评审（3.5/10），修复 ArticleReviewActions.tsx 相关问题：

| 编号 | 级别 | 修复内容 | 涉及文件 |
|------|------|----------|----------|
| B-1 | BLOCKING | 消除死代码：ArticleDetail 导入 ArticleReviewActions 替换内联审核代码 | ArticleReviewActions.tsx, ArticleContentEditor.tsx, ArticleDetail.tsx |
| B-2 | BLOCKING | 审核按钮添加权限控制：visible prop 由 permissions.canReview 控制 | ArticleReviewActions.tsx, ArticleDetail.tsx |
| H-1 | HIGH | 双重提交防护：添加 loading/disabled 状态，onReview 签名改为 Promise<void> | ArticleReviewActions.tsx |
| H-1b | HIGH | 自审排除：canReview 增加 created_by !== user.id 检查 | useArticlePermissions.ts |
| H-3 | HIGH | 审核审计日志：reviewArticle handler 添加 logger.info('article_reviewed') | article.controller.ts |
| M-1 | MEDIUM | antd Space 替代原生 div + inline style | ArticleReviewActions.tsx |
| M-2 | MEDIUM | 移除 Button size="small"，使用默认尺寸 | ArticleReviewActions.tsx |
| M-3 | MEDIUM | 文案提取为 TEXT 常量对象 | ArticleReviewActions.tsx |
| L-2 | LOW | 添加 displayName | ArticleReviewActions.tsx |
| 测试 | — | 编写 11 个单元测试覆盖全部场景 | ArticleReviewActions.test.tsx |

### article.service.d.ts 五维评审修复记录（2026-05-26）

根据安全评审（3.9/10 REQUEST CHANGES）+ Committer评审（5.5/10 CONDITIONAL APPROVE）+ 架构评审（5.2/10 CONDITIONAL APPROVE）+ 质量评审（6.8/10 CONDITIONAL APPROVE），修复 IArticleService 接口及实现：

| 编号 | 级别 | 修复内容 | 涉及文件 |
|------|------|----------|----------|
| C1/IDOR | CRITICAL | `getById` 添加 `projectId` 参数，校验文章归属项目 | article.service.ts, article.service.impl.ts |
| C1/IDOR | CRITICAL | `listVersions` 添加 `projectId` 参数，校验文章归属项目 | article.service.ts, article.service.impl.ts |
| C2/auth | HIGH | `list()` 方法 `auth` 参数从可选改为必填，移至 `search?`/`status?` 之前 | article.service.ts, article.service.impl.ts |
| C3/role | HIGH | `AuthContext.role` 从 `string` 改为 `Role` 联合类型 `'sysadmin' \| 'admin' \| 'view'` | types/auth.ts, constants/roles.ts |
| H1-H4 | HIGH | AuthContext 统一提取到 `apis/types/auth.ts`，所有消费者统一导入 | 多文件 |
| ISP | MEDIUM | 发布计划方法保留在 IPublishingScheduleService（Prisma schema 限制） | article.service.ts |
| 测试 | — | 更新 260 个测试用例适配接口签名变更 | article.service.test.ts, publishing-schedule.*.test.ts |

### ArticleImageManager.tsx 五维评审修复记录（2026-05-26）

根据安全评审（5.8/10 CONDITIONAL APPROVE）+ 架构评审（5.4/10 CONDITIONAL APPROVE）+ 质量评审（6.8/10 CONDITIONAL APPROVE）+ UI评审（4.5/10 CONDITIONAL APPROVE）+ Committer评审（5.6/10 CONDITIONAL APPROVE），修复 ArticleImageManager.tsx：

| 编号 | 级别 | 修复内容 | 涉及文件 |
|------|------|----------|----------|
| B-1 | BLOCKING | 添加 MAX_IMAGES=20 数量上限，防止 DoS | ArticleImageManager.tsx |
| B-2 | BLOCKING | 使用 useRef 修复闭包陈旧导致并发操作数据丢失 | ArticleImageManager.tsx |
| B-3 | BLOCKING | 37 处 inline style 提取为 CSS class（article-img-*），遵守 DESIGN.md Token 体系 | ArticleImageManager.tsx, global.css |
| B-4 | BLOCKING | 原生 span 替换为 antd Empty 组件 | ArticleImageManager.tsx |
| H-1 | HIGH | 删除按钮扩展至 32×32px，添加 Popconfirm 删除确认 | ArticleImageManager.tsx |
| H-2 | HIGH | Segmented 移除 size="small" | ArticleImageManager.tsx |
| H-3 | HIGH | 上传错误 catch 移除 getApiErrorMessage，直接显示"上传失败"，防止 4xx 信息泄露 | ArticleImageManager.tsx |
| H-5 | HIGH | 上传区域改用 Upload.Dragger 替代手动 div | ArticleImageManager.tsx |
| H-6 | HIGH | 移除 Segmented size="small"（与 H-2 合并） | ArticleImageManager.tsx |
| M-1 | MEDIUM | 知识库图片添加 CSS hover 反馈 | global.css |
| M-2 | MEDIUM | 删除按钮添加 Tooltip | ArticleImageManager.tsx |
| M-3 | MEDIUM | 知识库加载态改用 Skeleton.Image 替代 Spin | ArticleImageManager.tsx |
| M-5 | MEDIUM | 所有 Image 组件添加 alt 属性 | ArticleImageManager.tsx |
| M-7 | MEDIUM | 已选列表添加 Divider 视觉分隔 | ArticleImageManager.tsx |
| M-10 | MEDIUM | React key 从数组索引改为 url 去重 | ArticleImageManager.tsx |
| L-1 | LOW | Input.Search enterButton 从 LinkOutlined 改为 PlusOutlined | ArticleImageManager.tsx |
| 测试 | — | 编写 24 个单元测试覆盖全部场景 | ArticleImageManager.test.tsx |
