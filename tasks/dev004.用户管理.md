# dev004. 用户管理

> 状态：✅ 已完成

---

## 功能说明

管理系统用户账号。仅系统管理员可访问。

### 用户列表

- 地址：`/users`
- 仅系统管理员可访问
- 以卡片网格形式展示用户
- 支持搜索（按用户名或姓名）、按角色筛选、按状态筛选（启用/禁用）
- 分页展示
- 每张用户卡片显示：姓名、用户名、角色标签、启用/禁用状态开关
- 卡片右上角有编辑图标，点击弹出编辑表单
- 列表末尾有"添加用户"虚线卡片
- 不提供删除功能，通过启用/禁用开关管理用户状态

### 用户表单（新建/编辑）

弹窗形式，包含以下字段：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 用户名 | 文本输入 | 是（仅新建） | 登录账号，不可重复 |
| 密码 | 密码输入 | 是（仅新建） | 登录密码 |
| 姓名 | 文本输入 | 是 | 用户中文名 |
| 角色 | 下拉选择 | 是 | 系统管理员 / 运营者 / 查看者 |

### API 端点

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/users` | sysadmin | 获取用户列表（分页、搜索、筛选） |
| GET | `/api/users/:id` | sysadmin | 获取用户详情 |
| POST | `/api/users` | sysadmin | 创建用户 |
| PUT | `/api/users/:id` | sysadmin | 更新用户 |
| DELETE | `/api/users/:id` | sysadmin | 删除用户 |

---

## 业务规则

1. **仅系统管理员可操作** — 运营者和查看者访问用户管理 API 均返回 403
2. **用户不绑定单一公司** — 一个用户可能关联多家公司的多个项目，具体权限通过公司关联设置
3. **系统管理员账号受保护**：
   - 系统管理员的角色不可被修改
   - 系统管理员账号不可被删除
4. 用户名必须唯一，重复时返回冲突错误
5. 密码加密存储

---

## 验收标准

- [x] 系统管理员可以查看、创建、编辑、删除用户
- [x] 运营者和查看者访问用户管理返回 403
- [x] 系统管理员的角色不可被修改
- [x] 系统管理员账号不可被删除
- [x] 用户名重复时返回冲突错误
- [x] 必填字段为空时提示错误

---

## 代码评审修复记录（2026-05-24）

基于三份评审报告（软件质量/架构/安全）的 Committer 终审，修复以下问题：

### 必须修复项（全部完成）

| # | 问题 | 修复方案 |
|---|------|----------|
| FIX-1 | `updateUser` 零输入验证 | 引入 `updateUserSchema` Zod 验证（角色白名单 + 密码长度 + 姓名长度） |
| FIX-2 | `req.body` 整体传入 Service | Zod Schema `strict()` 模式自动过滤未定义字段 |
| FIX-3 | `pageSize` 无上限，存在 DoS 风险 | `listUsersSchema` 添加 `.max(100)` |
| FIX-4 | 未使用 `created()` 工具函数 | 导入 `created`，替换手动构造 201 响应 |
| FIX-5 | 异常通过 `err.message === '...'` 字符串匹配 | Service 层改用 `NotFoundError`/`ForbiddenError`/`ConflictError`，Controller 用 `instanceof` + `handleError` |

### 建议改进项（同时完成）

| # | 问题 | 修复方案 |
|---|------|----------|
| OPT-2 | `catch (err: any)` → `unknown` | 统一使用 `err: unknown` |
| OPT-3 | `errors.ts` 缺少 `ConflictError` | 补充 `ConflictError` 类用于 409 场景 |

### 修改文件

- `apis/errors.ts` — 新增 `ConflictError` 类
- `apis/schema/user.schema.ts` — 新建 Zod Schema（`listUsersSchema`/`createUserSchema`/`updateUserSchema`）
- `apis/service/impl/user.service.impl.ts` — 异常类替换 `throw new Error(...)` → `throw new NotFoundError(...)` 等
- `apis/controller/user.controller.ts` — 完全对齐 `todo.controller` 模式（`handleError` + Zod 验证 + `created()`）
- `tests/apis/user.controller.test.ts` — 适配 Zod 精确错误消息（75 测试全通过）

### 安全评审修复（2026-05-25）

基于 `tasks/review/user.controller.security.md` 安全评审报告，补全最后一项：

| # | 问题 | 修复方案 | 状态 |
|---|------|----------|------|
| SEC-L-01 | `username` 无 regex 防护，可存储 XSS 字符 | 添加 `.regex(/^[a-zA-Z0-9_]+$/)` 限制 | ✅ 已修复 |

其余 9 项（SEC-H-01/H-02/M-01~04/L-02~04）已在架构评审修复中完成。安全评级从 ⚠️ MEDIUM 提升至 ✅ LOW，89 测试全通过。

### TDD 第二轮四维补全（2026-05-25）

基于四维覆盖标准，从89用例扩展至188用例（+99），100% Stmts/Branch/Funcs/Lines 覆盖率不变：

| 维度 | 新增用例 | 说明 |
|------|----------|------|
| 错误类型多样性 | 15 | 所有端点 × 所有5种错误类型（ZodError/NotFoundError/ForbiddenError/ConflictError/generic） |
| 安全注入 | 25 | ID注入/路径穿越/XSS/SQL注入/CRLF/Unicode/Token安全/Content-Type |
| 边界值 | 30 | ID/page/pageSize/username/cn_name/password/role/search/status/company_id 边界 |
| 响应结构 | 8 | 成功/错误响应code+message验证 |
| 角色矩阵 | 16 | view/admin/sysadmin × 5端点 + Token篡改 |
| 并发竞态 | 2 | 并发list + 同名竞态创建 |
| HTTP方法安全 | 4 | PATCH/PUT/DELETE/POST非法方法组合 |
