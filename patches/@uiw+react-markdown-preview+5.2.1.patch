diff --git a/node_modules/@uiw/react-markdown-preview/esm/Props.d.ts b/node_modules/@uiw/react-markdown-preview/esm/Props.d.ts
index b550d96..cab60d1 100644
--- a/node_modules/@uiw/react-markdown-preview/esm/Props.d.ts
+++ b/node_modules/@uiw/react-markdown-preview/esm/Props.d.ts
@@ -1,27 +1,75 @@
 import { type Options } from 'react-markdown';
 import { type RehypeRewriteOptions } from 'rehype-rewrite';
 import { type PluggableList } from 'unified';
+/**
+ * 管线配置接口（A-03 OCP 修复）。
+ * 允许在默认管线的头部/尾部插入自定义插件，无需修改源码。
+ */
+export interface PipelineConfig {
+    /** 在默认管线之前插入（reservedMeta 之前） */
+    prepend?: PluggableList;
+    /** 在默认管线之后追加（rehypePrism 之后 / nohighlight 最后默认插件之后） */
+    append?: PluggableList;
+}
+/**
+ * ⚠️ 安全说明（SEC-MD-04）：本接口继承 react-markdown Options，
+ * 暴露 allowElement/rehypePlugins/remarkPlugins/urlTransform 等危险属性。
+ * 渲染用户提交内容时，必须配合 allowElement 白名单 + DOMPurify 消毒使用。
+ */
 export interface MarkdownPreviewProps extends Omit<Options, 'children'> {
     prefixCls?: string;
     className?: string;
+    /**
+     * ⚠️ 安全警告（SEC-MD-01）：
+     * 无长度限制（DoS风险）+ 无HTML过滤（XSS风险）。
+     * 建议对不可信输入进行服务端消毒（DOMPurify），并限制长度上限（建议 1MB）。
+     */
     source?: string;
     disableCopy?: boolean;
     style?: React.CSSProperties;
+    /**
+     * ⚠️ 安全警告（SEC-MD-03）：
+     * 此回调可移除安全过滤插件（如 rehype-sanitize），导致 XSS 风险。
+     */
     pluginsFilter?: (type: 'rehype' | 'remark', plugin: PluggableList) => PluggableList;
-    wrapperElement?: React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement> & {
+    /** SEC-MD-05: 收窄类型，排除 dangerouslySetInnerHTML 和事件处理器 */
+    wrapperElement?: {
+        className?: string;
+        style?: React.CSSProperties;
+        id?: string;
+        role?: string;
         'data-color-mode'?: 'light' | 'dark';
     };
     /**
      * Please use wrapperElement, Will be removed in v5 release.
      * @deprecated
      */
-    warpperElement?: React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement> & {
+    warpperElement?: {
+        className?: string;
+        style?: React.CSSProperties;
+        id?: string;
+        role?: string;
         'data-color-mode'?: 'light' | 'dark';
     };
     onScroll?: (e: React.UIEvent<HTMLDivElement>) => void;
     onMouseOver?: (e: React.MouseEvent<HTMLDivElement>) => void;
+    /**
+     * ⚠️ 安全警告（SEC-MD-02）：
+     * 此回调运行在安全过滤之后，可以覆盖安全过滤结果。
+     * 仅在完全信任输入源时使用。请勿用于渲染用户提交的 Markdown 内容。
+     */
     rehypeRewrite?: RehypeRewriteOptions['rewrite'];
+    /**
+     * 管线配置（A-03 OCP 修复）。
+     * 允许在默认插件管线的头部/尾部插入自定义插件，无需修改源码。
+     */
+    pipeline?: PipelineConfig;
 }
+/**
+ * ⚠️ 安全说明（SEC-MD-06）：继承全部 Props 属性，
+ * mdp 为内部 div 的直接 DOM 引用，可进行任意 DOM 操作。
+ * 建议仅在安全的命令式场景中使用（如 scrollTo）。
+ */
 export interface MarkdownPreviewRef extends MarkdownPreviewProps {
     mdp: React.RefObject<HTMLDivElement>;
 }
diff --git a/node_modules/@uiw/react-markdown-preview/esm/common.d.ts b/node_modules/@uiw/react-markdown-preview/esm/common.d.ts
index 85d7c47..d56177d 100644
--- a/node_modules/@uiw/react-markdown-preview/esm/common.d.ts
+++ b/node_modules/@uiw/react-markdown-preview/esm/common.d.ts
@@ -1,5 +1,15 @@
 import React from 'react';
-import type { MarkdownPreviewProps, MarkdownPreviewRef } from './Props';
-export * from './Props';
-declare const _default: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
-export default _default;
+import type { PluggableList } from 'unified';
+import type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+export type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+/**
+ * A-02: 纯函数构建 common rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ */
+export declare function buildCommonPipeline(options: {
+    rewriteFn: (node: any, index?: number | undefined, parent?: any) => void;
+    userPlugins?: PluggableList;
+    pipeline?: PipelineConfig;
+}): PluggableList;
+declare const MarkdownPreviewHighlighted: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
+export default MarkdownPreviewHighlighted;
diff --git a/node_modules/@uiw/react-markdown-preview/esm/common.js b/node_modules/@uiw/react-markdown-preview/esm/common.js
index 3201d50..800dd02 100644
--- a/node_modules/@uiw/react-markdown-preview/esm/common.js
+++ b/node_modules/@uiw/react-markdown-preview/esm/common.js
@@ -1,5 +1,5 @@
 import _extends from "@babel/runtime/helpers/extends";
-import React from 'react';
+import React, { useMemo as _useMemo } from 'react';
 import rehypePrism from 'rehype-prism-plus/common';
 import rehypeRewrite from 'rehype-rewrite';
 import rehypeAttrs from 'rehype-attr';
@@ -10,17 +10,58 @@ import { retrieveMeta } from "./plugins/retrieveMeta.js";
 import { rehypeRewriteHandle, defaultRehypePlugins } from "./rehypePlugins.js";
 import { jsx as _jsx } from "react/jsx-runtime";
 export * from "./Props.js";
-export default /*#__PURE__*/React.forwardRef((props, ref) => {
-  var _props$disableCopy;
-  var rehypePlugins = [reservedMeta, rehypeRaw, retrieveMeta, ...defaultRehypePlugins, [rehypeRewrite, {
-    rewrite: rehypeRewriteHandle((_props$disableCopy = props.disableCopy) != null ? _props$disableCopy : false, props.rehypeRewrite)
-  }], [rehypeAttrs, {
-    properties: 'attr'
-  }], ...(props.rehypePlugins || []), [rehypePrism, {
+// A-07/A-02: 共享插件配置常量（A-07 消除硬编码分散）
+var REHYPE_ATTRS_CONFIG = [rehypeAttrs, { properties: 'attr' }];
+/**
+ * A-02: 纯函数构建 common rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ *
+ * 管线顺序（严格，不可随意调整）：
+ *   1. pipeline.prepend    — 用户自定义前缀插件（A-03 OCP）
+ *   2. reservedMeta        — 保留元数据
+ *   3. rehypeRaw           — HTML 解析
+ *   4. retrieveMeta        — 恢复元数据
+ *   5. defaultRehypePlugins — slug/headings/ignore
+ *   6. rehypeRewrite       — AST 重写（代码复制按钮）
+ *   7. rehypeAttrs         — 属性转换（A-07 共享常量）
+ *   8. userPlugins         — 用户自定义插件
+ *   9. rehypePrism         — 代码语法高亮
+ *  10. pipeline.append     — 用户自定义后缀插件（A-03 OCP）
+ * 注意：preview.tsx 在 skipHtml=false 时也会 push raw，可能导致双重 rehype-raw（Q-04）
+ */
+export function buildCommonPipeline(options) {
+  return [].concat(options.pipeline?.prepend ?? [], [reservedMeta, rehypeRaw, retrieveMeta], defaultRehypePlugins, [[rehypeRewrite, {
+    rewrite: options.rewriteFn
+  }], REHYPE_ATTRS_CONFIG], options.userPlugins ?? [], [[rehypePrism, {
     ignoreMissing: true
-  }]];
-  return /*#__PURE__*/_jsx(MarkdownPreview, _extends({}, props, {
+  }]], options.pipeline?.append ?? []);
+}
+// Q-01/Q-05 性能修复：useMemo 稳定插件数组 + rehypeRewriteHandle 闭包
+var MarkdownPreviewHighlighted = /*#__PURE__*/React.forwardRef(function (props, ref) {
+  // A-03: 解构已消费的 props，消除幽灵 prop 泄漏到下游 preview.tsx
+  var disableCopy = props.disableCopy;
+  var userRewrite = props.rehypeRewrite;
+  var userPlugins = props.rehypePlugins;
+  var pipeline = props.pipeline;
+  var restProps = _extends({}, props);
+  delete restProps.disableCopy;
+  delete restProps.rehypeRewrite;
+  delete restProps.rehypePlugins;
+  delete restProps.pipeline;
+  // Q-05: 稳定 rewrite 闭包
+  var rewriteFn = _useMemo(function () {
+    return rehypeRewriteHandle(disableCopy != null ? disableCopy : false, userRewrite);
+  }, [disableCopy, userRewrite]);
+  // A-02: 插件链由纯函数 buildCommonPipeline 构建（可独立测试）
+  // Q-01: 稳定插件数组
+  var rehypePlugins = _useMemo(function () {
+    return buildCommonPipeline({ rewriteFn: rewriteFn, userPlugins: userPlugins, pipeline: pipeline });
+  }, [rewriteFn, userPlugins, pipeline]);
+  // A-03: 仅透传未消费的 props
+  return /*#__PURE__*/_jsx(MarkdownPreview, _extends({}, restProps, {
     rehypePlugins: rehypePlugins,
     ref: ref
   }));
 });
+MarkdownPreviewHighlighted.displayName = 'MarkdownPreviewHighlighted';
+export default MarkdownPreviewHighlighted;
diff --git a/node_modules/@uiw/react-markdown-preview/esm/index.d.ts b/node_modules/@uiw/react-markdown-preview/esm/index.d.ts
index 85d7c47..5587d7d 100644
--- a/node_modules/@uiw/react-markdown-preview/esm/index.d.ts
+++ b/node_modules/@uiw/react-markdown-preview/esm/index.d.ts
@@ -1,5 +1,15 @@
 import React from 'react';
-import type { MarkdownPreviewProps, MarkdownPreviewRef } from './Props';
-export * from './Props';
-declare const _default: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
-export default _default;
+import type { PluggableList } from 'unified';
+import type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+export type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+/**
+ * A-02: 纯函数构建 index（完整版） rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ */
+export declare function buildFullPipeline(options: {
+    rewriteFn: (node: any, index?: number | undefined, parent?: any) => void;
+    userPlugins?: PluggableList;
+    pipeline?: PipelineConfig;
+}): PluggableList;
+declare const MarkdownPreviewFull: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
+export default MarkdownPreviewFull;
diff --git a/node_modules/@uiw/react-markdown-preview/esm/index.js b/node_modules/@uiw/react-markdown-preview/esm/index.js
index 397cab4..e97d91f 100644
--- a/node_modules/@uiw/react-markdown-preview/esm/index.js
+++ b/node_modules/@uiw/react-markdown-preview/esm/index.js
@@ -1,5 +1,5 @@
 import _extends from "@babel/runtime/helpers/extends";
-import React from 'react';
+import React, { useMemo as _useMemo } from 'react';
 import rehypePrism from 'rehype-prism-plus';
 import rehypeRewrite from 'rehype-rewrite';
 import rehypeAttrs from 'rehype-attr';
@@ -10,17 +10,57 @@ import { retrieveMeta } from "./plugins/retrieveMeta.js";
 import { rehypeRewriteHandle, defaultRehypePlugins } from "./rehypePlugins.js";
 import { jsx as _jsx } from "react/jsx-runtime";
 export * from "./Props.js";
-export default /*#__PURE__*/React.forwardRef((props, ref) => {
-  var _props$disableCopy;
-  var rehypePlugins = [reservedMeta, rehypeRaw, retrieveMeta, ...defaultRehypePlugins, [rehypeRewrite, {
-    rewrite: rehypeRewriteHandle((_props$disableCopy = props.disableCopy) != null ? _props$disableCopy : false, props.rehypeRewrite)
-  }], [rehypeAttrs, {
-    properties: 'attr'
-  }], ...(props.rehypePlugins || []), [rehypePrism, {
+// A-07/A-02: 共享插件配置常量（A-07 消除硬编码分散）
+var REHYPE_ATTRS_CONFIG = [rehypeAttrs, { properties: 'attr' }];
+/**
+ * A-02: 纯函数构建 index（完整版） rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ *
+ * 管线顺序（严格，不可随意调整）：
+ *   1. pipeline.prepend    — 用户自定义前缀插件（A-03 OCP）
+ *   2. reservedMeta        — 保留元数据
+ *   3. rehypeRaw           — HTML 解析
+ *   4. retrieveMeta        — 恢复元数据
+ *   5. defaultRehypePlugins — slug/headings/ignore
+ *   6. rehypeRewrite       — AST 重写（代码复制按钮）
+ *   7. rehypeAttrs         — 属性转换（A-07 共享常量）
+ *   8. userPlugins         — 用户自定义插件
+ *   9. rehypePrism         — 代码语法高亮
+ *  10. pipeline.append     — 用户自定义后缀插件（A-03 OCP）
+ */
+export function buildFullPipeline(options) {
+  return [].concat(options.pipeline?.prepend ?? [], [reservedMeta, rehypeRaw, retrieveMeta], defaultRehypePlugins, [[rehypeRewrite, {
+    rewrite: options.rewriteFn
+  }], REHYPE_ATTRS_CONFIG], options.userPlugins ?? [], [[rehypePrism, {
     ignoreMissing: true
-  }]];
-  return /*#__PURE__*/_jsx(MarkdownPreview, _extends({}, props, {
+  }]], options.pipeline?.append ?? []);
+}
+// Q-01/Q-05 性能修复：useMemo 稳定插件数组 + rehypeRewriteHandle 闭包
+var MarkdownPreviewFull = /*#__PURE__*/React.forwardRef(function (props, ref) {
+  // A-03: 解构已消费的 props，消除幽灵 prop 泄漏到下游 preview.tsx
+  var disableCopy = props.disableCopy;
+  var userRewrite = props.rehypeRewrite;
+  var userPlugins = props.rehypePlugins;
+  var pipeline = props.pipeline;
+  var restProps = _extends({}, props);
+  delete restProps.disableCopy;
+  delete restProps.rehypeRewrite;
+  delete restProps.rehypePlugins;
+  delete restProps.pipeline;
+  // Q-05: 稳定 rewrite 闭包
+  var rewriteFn = _useMemo(function () {
+    return rehypeRewriteHandle(disableCopy != null ? disableCopy : false, userRewrite);
+  }, [disableCopy, userRewrite]);
+  // A-02: 插件链由纯函数 buildFullPipeline 构建（可独立测试）
+  // Q-01: 稳定插件数组
+  var rehypePlugins = _useMemo(function () {
+    return buildFullPipeline({ rewriteFn: rewriteFn, userPlugins: userPlugins, pipeline: pipeline });
+  }, [rewriteFn, userPlugins, pipeline]);
+  // A-03: 仅透传未消费的 props
+  return /*#__PURE__*/_jsx(MarkdownPreview, _extends({}, restProps, {
     rehypePlugins: rehypePlugins,
     ref: ref
   }));
 });
+MarkdownPreviewFull.displayName = 'MarkdownPreviewFull';
+export default MarkdownPreviewFull;
diff --git a/node_modules/@uiw/react-markdown-preview/esm/nohighlight.d.ts b/node_modules/@uiw/react-markdown-preview/esm/nohighlight.d.ts
index 85d7c47..0b89211 100644
--- a/node_modules/@uiw/react-markdown-preview/esm/nohighlight.d.ts
+++ b/node_modules/@uiw/react-markdown-preview/esm/nohighlight.d.ts
@@ -1,5 +1,15 @@
 import React from 'react';
-import type { MarkdownPreviewProps, MarkdownPreviewRef } from './Props';
-export * from './Props';
-declare const _default: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
-export default _default;
+import type { PluggableList } from 'unified';
+import type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+export type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+/**
+ * A-02: 纯函数构建 nohighlight rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ */
+export declare function buildNoHighlightPipeline(options: {
+    rewriteFn: (node: any, index?: number | undefined, parent?: any) => void;
+    userPlugins?: PluggableList;
+    pipeline?: PipelineConfig;
+}): PluggableList;
+declare const NoHighlightMarkdown: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
+export default NoHighlightMarkdown;
diff --git a/node_modules/@uiw/react-markdown-preview/esm/nohighlight.js b/node_modules/@uiw/react-markdown-preview/esm/nohighlight.js
index 5399bb6..20344d1 100644
--- a/node_modules/@uiw/react-markdown-preview/esm/nohighlight.js
+++ b/node_modules/@uiw/react-markdown-preview/esm/nohighlight.js
@@ -1,5 +1,5 @@
 import _extends from "@babel/runtime/helpers/extends";
-import React from 'react';
+import React, { useMemo as _useMemo } from 'react';
 import MarkdownPreview from "./preview.js";
 import rehypeRewrite from 'rehype-rewrite';
 import rehypeAttrs from 'rehype-attr';
@@ -8,15 +8,53 @@ import { retrieveMeta } from "./plugins/retrieveMeta.js";
 import { rehypeRewriteHandle, defaultRehypePlugins } from "./rehypePlugins.js";
 import { jsx as _jsx } from "react/jsx-runtime";
 export * from "./Props.js";
-export default /*#__PURE__*/React.forwardRef((props, ref) => {
-  var _props$disableCopy;
-  var rehypePlugins = [reservedMeta, retrieveMeta, ...defaultRehypePlugins, [rehypeRewrite, {
-    rewrite: rehypeRewriteHandle((_props$disableCopy = props.disableCopy) != null ? _props$disableCopy : false, props.rehypeRewrite)
-  }], [rehypeAttrs, {
-    properties: 'attr'
-  }], ...(props.rehypePlugins || [])];
-  return /*#__PURE__*/_jsx(MarkdownPreview, _extends({}, props, {
+// A-07/A-02: 共享插件配置常量（A-07 消除硬编码分散）
+var REHYPE_ATTRS_CONFIG = [rehypeAttrs, { properties: 'attr' }];
+/**
+ * A-02: 纯函数构建 nohighlight rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ *
+ * 管线顺序（严格，不可随意调整）：
+ *   1. pipeline.prepend    — 用户自定义前缀插件（A-03 OCP）
+ *   2. reservedMeta        — 保留元数据
+ *   3. retrieveMeta        — 恢复元数据
+ *   4. defaultRehypePlugins — slug/headings/ignore
+ *   5. rehypeRewrite       — AST 重写（代码复制按钮）
+ *   6. rehypeAttrs         — 属性转换（A-07 共享常量）
+ *   7. userPlugins         — 用户自定义插件
+ *   8. pipeline.append     — 用户自定义后缀插件（A-03 OCP）
+ */
+export function buildNoHighlightPipeline(options) {
+  return [].concat(options.pipeline?.prepend ?? [], [reservedMeta, retrieveMeta], defaultRehypePlugins, [[rehypeRewrite, {
+    rewrite: options.rewriteFn
+  }], REHYPE_ATTRS_CONFIG], options.userPlugins ?? [], options.pipeline?.append ?? []);
+}
+// Q-01/Q-05 性能修复：useMemo 稳定插件数组 + rehypeRewriteHandle 闭包
+var NoHighlightMarkdown = /*#__PURE__*/React.forwardRef(function (props, ref) {
+  // A-03: 解构已消费的 props，消除幽灵 prop 泄漏到下游 preview.tsx
+  var disableCopy = props.disableCopy;
+  var userRewrite = props.rehypeRewrite;
+  var userPlugins = props.rehypePlugins;
+  var pipeline = props.pipeline;
+  var restProps = _extends({}, props);
+  delete restProps.disableCopy;
+  delete restProps.rehypeRewrite;
+  delete restProps.rehypePlugins;
+  delete restProps.pipeline;
+  // Q-05: 稳定 rewrite 闭包
+  var rewriteFn = _useMemo(function () {
+    return rehypeRewriteHandle(disableCopy != null ? disableCopy : false, userRewrite);
+  }, [disableCopy, userRewrite]);
+  // A-02: 插件链由纯函数 buildNoHighlightPipeline 构建（可独立测试）
+  // Q-01: 稳定插件数组
+  var rehypePlugins = _useMemo(function () {
+    return buildNoHighlightPipeline({ rewriteFn: rewriteFn, userPlugins: userPlugins, pipeline: pipeline });
+  }, [rewriteFn, userPlugins, pipeline]);
+  // A-03: 仅透传未消费的 props
+  return /*#__PURE__*/_jsx(MarkdownPreview, _extends({}, restProps, {
     rehypePlugins: rehypePlugins,
     ref: ref
   }));
 });
+NoHighlightMarkdown.displayName = 'NoHighlightMarkdown';
+export default NoHighlightMarkdown;
diff --git a/node_modules/@uiw/react-markdown-preview/esm/rehypePlugins.js b/node_modules/@uiw/react-markdown-preview/esm/rehypePlugins.js
index 2796334..bc3dea0 100644
--- a/node_modules/@uiw/react-markdown-preview/esm/rehypePlugins.js
+++ b/node_modules/@uiw/react-markdown-preview/esm/rehypePlugins.js
@@ -5,20 +5,41 @@ import rehypeIgnore from 'rehype-ignore';
 import { getCodeString } from 'rehype-rewrite';
 import { octiconLink } from "./nodes/octiconLink.js";
 import { copyElement } from "./nodes/copy.js";
+var HEADING_TAGS = new Set(['h1', 'h2', 'h3', 'h4', 'h5', 'h6']);
+var MAX_CODE_LENGTH = 100_000;
+var SAFE_ANCHOR_PROPS = ['ariaHidden', 'href'];
+function escapeHtmlAttr(str) {
+  return str.replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
+}
 export var rehypeRewriteHandle = (disableCopy, rewrite) => (node, index, parent) => {
-  if (node.type === 'element' && parent && parent.type === 'root' && /h(1|2|3|4|5|6)/.test(node.tagName)) {
+  if (node.type === 'element' && parent && parent.type === 'root' && HEADING_TAGS.has(node.tagName)) {
     var child = node.children && node.children[0];
-    if (child && child.properties && child.properties.ariaHidden === 'true') {
-      child.properties = _extends({
-        class: 'anchor'
-      }, child.properties);
+    if (child && child.type === 'element' && child.properties && child.properties.ariaHidden === 'true') {
+      var safeProps = { class: 'anchor' };
+      for (var _i = 0; _i < SAFE_ANCHOR_PROPS.length; _i++) {
+        var key = SAFE_ANCHOR_PROPS[_i];
+        if (child.properties[key] !== undefined) {
+          safeProps[key] = child.properties[key];
+        }
+      }
+      child.properties = safeProps;
       child.children = [octiconLink];
     }
   }
   if (node.type === 'element' && node.tagName === 'pre' && !disableCopy) {
     var code = getCodeString(node.children);
-    node.children.push(copyElement(code));
+    if (code.length <= MAX_CODE_LENGTH) {
+      node.children.push(copyElement(escapeHtmlAttr(code)));
+    }
+  }
+  if (rewrite) {
+    try {
+      rewrite(node, index === null ? undefined : index, parent === null ? undefined : parent);
+    } catch (err) {
+      if (typeof console !== 'undefined') {
+        console.warn('[rehypeRewriteHandle] User rewrite callback error:', err);
+      }
+    }
   }
-  rewrite && rewrite(node, index === null ? undefined : index, parent === null ? undefined : parent);
 };
 export var defaultRehypePlugins = [slug, headings, rehypeIgnore];
\ No newline at end of file
diff --git a/node_modules/@uiw/react-markdown-preview/lib/Props.d.ts b/node_modules/@uiw/react-markdown-preview/lib/Props.d.ts
index b550d96..cab60d1 100644
--- a/node_modules/@uiw/react-markdown-preview/lib/Props.d.ts
+++ b/node_modules/@uiw/react-markdown-preview/lib/Props.d.ts
@@ -1,27 +1,75 @@
 import { type Options } from 'react-markdown';
 import { type RehypeRewriteOptions } from 'rehype-rewrite';
 import { type PluggableList } from 'unified';
+/**
+ * 管线配置接口（A-03 OCP 修复）。
+ * 允许在默认管线的头部/尾部插入自定义插件，无需修改源码。
+ */
+export interface PipelineConfig {
+    /** 在默认管线之前插入（reservedMeta 之前） */
+    prepend?: PluggableList;
+    /** 在默认管线之后追加（rehypePrism 之后 / nohighlight 最后默认插件之后） */
+    append?: PluggableList;
+}
+/**
+ * ⚠️ 安全说明（SEC-MD-04）：本接口继承 react-markdown Options，
+ * 暴露 allowElement/rehypePlugins/remarkPlugins/urlTransform 等危险属性。
+ * 渲染用户提交内容时，必须配合 allowElement 白名单 + DOMPurify 消毒使用。
+ */
 export interface MarkdownPreviewProps extends Omit<Options, 'children'> {
     prefixCls?: string;
     className?: string;
+    /**
+     * ⚠️ 安全警告（SEC-MD-01）：
+     * 无长度限制（DoS风险）+ 无HTML过滤（XSS风险）。
+     * 建议对不可信输入进行服务端消毒（DOMPurify），并限制长度上限（建议 1MB）。
+     */
     source?: string;
     disableCopy?: boolean;
     style?: React.CSSProperties;
+    /**
+     * ⚠️ 安全警告（SEC-MD-03）：
+     * 此回调可移除安全过滤插件（如 rehype-sanitize），导致 XSS 风险。
+     */
     pluginsFilter?: (type: 'rehype' | 'remark', plugin: PluggableList) => PluggableList;
-    wrapperElement?: React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement> & {
+    /** SEC-MD-05: 收窄类型，排除 dangerouslySetInnerHTML 和事件处理器 */
+    wrapperElement?: {
+        className?: string;
+        style?: React.CSSProperties;
+        id?: string;
+        role?: string;
         'data-color-mode'?: 'light' | 'dark';
     };
     /**
      * Please use wrapperElement, Will be removed in v5 release.
      * @deprecated
      */
-    warpperElement?: React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement> & {
+    warpperElement?: {
+        className?: string;
+        style?: React.CSSProperties;
+        id?: string;
+        role?: string;
         'data-color-mode'?: 'light' | 'dark';
     };
     onScroll?: (e: React.UIEvent<HTMLDivElement>) => void;
     onMouseOver?: (e: React.MouseEvent<HTMLDivElement>) => void;
+    /**
+     * ⚠️ 安全警告（SEC-MD-02）：
+     * 此回调运行在安全过滤之后，可以覆盖安全过滤结果。
+     * 仅在完全信任输入源时使用。请勿用于渲染用户提交的 Markdown 内容。
+     */
     rehypeRewrite?: RehypeRewriteOptions['rewrite'];
+    /**
+     * 管线配置（A-03 OCP 修复）。
+     * 允许在默认插件管线的头部/尾部插入自定义插件，无需修改源码。
+     */
+    pipeline?: PipelineConfig;
 }
+/**
+ * ⚠️ 安全说明（SEC-MD-06）：继承全部 Props 属性，
+ * mdp 为内部 div 的直接 DOM 引用，可进行任意 DOM 操作。
+ * 建议仅在安全的命令式场景中使用（如 scrollTo）。
+ */
 export interface MarkdownPreviewRef extends MarkdownPreviewProps {
     mdp: React.RefObject<HTMLDivElement>;
 }
diff --git a/node_modules/@uiw/react-markdown-preview/lib/common.d.ts b/node_modules/@uiw/react-markdown-preview/lib/common.d.ts
index 85d7c47..d56177d 100644
--- a/node_modules/@uiw/react-markdown-preview/lib/common.d.ts
+++ b/node_modules/@uiw/react-markdown-preview/lib/common.d.ts
@@ -1,5 +1,15 @@
 import React from 'react';
-import type { MarkdownPreviewProps, MarkdownPreviewRef } from './Props';
-export * from './Props';
-declare const _default: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
-export default _default;
+import type { PluggableList } from 'unified';
+import type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+export type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+/**
+ * A-02: 纯函数构建 common rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ */
+export declare function buildCommonPipeline(options: {
+    rewriteFn: (node: any, index?: number | undefined, parent?: any) => void;
+    userPlugins?: PluggableList;
+    pipeline?: PipelineConfig;
+}): PluggableList;
+declare const MarkdownPreviewHighlighted: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
+export default MarkdownPreviewHighlighted;
diff --git a/node_modules/@uiw/react-markdown-preview/lib/common.js b/node_modules/@uiw/react-markdown-preview/lib/common.js
index b911908..a9434d6 100644
--- a/node_modules/@uiw/react-markdown-preview/lib/common.js
+++ b/node_modules/@uiw/react-markdown-preview/lib/common.js
@@ -1,10 +1,11 @@
 "use strict";
 
 var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault")["default"];
-Object.defineProperty(exports, "__esModule", {
+Object.defineProperty(exports, "esModule", {
   value: true
 });
 var _exportNames = {};
+exports.buildCommonPipeline = buildCommonPipeline;
 exports["default"] = void 0;
 var _objectSpread2 = _interopRequireDefault(require("@babel/runtime/helpers/objectSpread2"));
 var _toConsumableArray2 = _interopRequireDefault(require("@babel/runtime/helpers/toConsumableArray"));
@@ -30,17 +31,59 @@ Object.keys(_Props).forEach(function (key) {
     }
   });
 });
-var _default = exports["default"] = /*#__PURE__*/_react["default"].forwardRef(function (props, ref) {
-  var _props$disableCopy;
-  var rehypePlugins = [_reservedMeta.reservedMeta, _rehypeRaw["default"], _retrieveMeta.retrieveMeta].concat((0, _toConsumableArray2["default"])(_rehypePlugins.defaultRehypePlugins), [[_rehypeRewrite["default"], {
-    rewrite: (0, _rehypePlugins.rehypeRewriteHandle)((_props$disableCopy = props.disableCopy) !== null && _props$disableCopy !== void 0 ? _props$disableCopy : false, props.rehypeRewrite)
-  }], [_rehypeAttr["default"], {
-    properties: 'attr'
-  }]], (0, _toConsumableArray2["default"])(props.rehypePlugins || []), [[_common["default"], {
+// A-07/A-02: 共享插件配置常量（A-07 消除硬编码分散）
+var REHYPE_ATTRS_CONFIG = [_rehypeAttr["default"], { properties: 'attr' }];
+/**
+ * A-02: 纯函数构建 common rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ *
+ * 管线顺序（严格，不可随意调整）：
+ *   1. pipeline.prepend    — 用户自定义前缀插件（A-03 OCP）
+ *   2. reservedMeta        — 保留元数据
+ *   3. rehypeRaw           — HTML 解析
+ *   4. retrieveMeta        — 恢复元数据
+ *   5. defaultRehypePlugins — slug/headings/ignore
+ *   6. rehypeRewrite       — AST 重写（代码复制按钮）
+ *   7. rehypeAttrs         — 属性转换（A-07 共享常量）
+ *   8. userPlugins         — 用户自定义插件
+ *   9. rehypePrism         — 代码语法高亮
+ *  10. pipeline.append     — 用户自定义后缀插件（A-03 OCP）
+ * 注意：preview.tsx 在 skipHtml=false 时也会 push raw，可能导致双重 rehype-raw（Q-04）
+ */
+function buildCommonPipeline(options) {
+  var _options$pipeline, _options$userPlugins, _options$pipeline2;
+  return [].concat((0, _toConsumableArray2["default"])((_options$pipeline = options.pipeline) === null || _options$pipeline === void 0 ? void 0 : _options$pipeline.prepend ?? []), [_reservedMeta.reservedMeta, _rehypeRaw["default"], _retrieveMeta.retrieveMeta], (0, _toConsumableArray2["default"])(_rehypePlugins.defaultRehypePlugins), [[_rehypeRewrite["default"], {
+    rewrite: options.rewriteFn
+  }], REHYPE_ATTRS_CONFIG], (0, _toConsumableArray2["default"])((_options$userPlugins = options.userPlugins) !== null && _options$userPlugins !== void 0 ? _options$userPlugins : []), [[_common["default"], {
     ignoreMissing: true
-  }]]);
-  return /*#__PURE__*/(0, _jsxRuntime.jsx)(_preview["default"], (0, _objectSpread2["default"])((0, _objectSpread2["default"])({}, props), {}, {
+  }]], (0, _toConsumableArray2["default"])((_options$pipeline2 = options.pipeline) === null || _options$pipeline2 === void 0 ? void 0 : _options$pipeline2.append ?? []));
+}
+// Q-01/Q-05 性能修复：useMemo 稳定插件数组 + rehypeRewriteHandle 闭包
+var MarkdownPreviewHighlighted = /*#__PURE__*/_react["default"].forwardRef(function (props, ref) {
+  // A-03: 解构已消费的 props，消除幽灵 prop 泄漏到下游 preview.tsx
+  var disableCopy = props.disableCopy;
+  var userRewrite = props.rehypeRewrite;
+  var userPlugins = props.rehypePlugins;
+  var pipeline = props.pipeline;
+  var restProps = (0, _objectSpread2["default"])({}, props);
+  delete restProps.disableCopy;
+  delete restProps.rehypeRewrite;
+  delete restProps.rehypePlugins;
+  delete restProps.pipeline;
+  // Q-05: 稳定 rewrite 闭包
+  var rewriteFn = _react["default"].useMemo(function () {
+    return (0, _rehypePlugins.rehypeRewriteHandle)(disableCopy !== null && disableCopy !== void 0 ? disableCopy : false, userRewrite);
+  }, [disableCopy, userRewrite]);
+  // A-02: 插件链由纯函数 buildCommonPipeline 构建（可独立测试）
+  // Q-01: 稳定插件数组
+  var rehypePlugins = _react["default"].useMemo(function () {
+    return buildCommonPipeline({ rewriteFn: rewriteFn, userPlugins: userPlugins, pipeline: pipeline });
+  }, [rewriteFn, userPlugins, pipeline]);
+  // A-03: 仅透传未消费的 props
+  return /*#__PURE__*/(0, _jsxRuntime.jsx)(_preview["default"], (0, _objectSpread2["default"])((0, _objectSpread2["default"])({}, restProps), {}, {
     rehypePlugins: rehypePlugins,
     ref: ref
   }));
 });
+MarkdownPreviewHighlighted.displayName = 'MarkdownPreviewHighlighted';
+var _default = exports["default"] = MarkdownPreviewHighlighted;
diff --git a/node_modules/@uiw/react-markdown-preview/lib/index.d.ts b/node_modules/@uiw/react-markdown-preview/lib/index.d.ts
index 85d7c47..5587d7d 100644
--- a/node_modules/@uiw/react-markdown-preview/lib/index.d.ts
+++ b/node_modules/@uiw/react-markdown-preview/lib/index.d.ts
@@ -1,5 +1,15 @@
 import React from 'react';
-import type { MarkdownPreviewProps, MarkdownPreviewRef } from './Props';
-export * from './Props';
-declare const _default: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
-export default _default;
+import type { PluggableList } from 'unified';
+import type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+export type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+/**
+ * A-02: 纯函数构建 index（完整版） rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ */
+export declare function buildFullPipeline(options: {
+    rewriteFn: (node: any, index?: number | undefined, parent?: any) => void;
+    userPlugins?: PluggableList;
+    pipeline?: PipelineConfig;
+}): PluggableList;
+declare const MarkdownPreviewFull: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
+export default MarkdownPreviewFull;
diff --git a/node_modules/@uiw/react-markdown-preview/lib/index.js b/node_modules/@uiw/react-markdown-preview/lib/index.js
index ff5212d..62fc5f6 100644
--- a/node_modules/@uiw/react-markdown-preview/lib/index.js
+++ b/node_modules/@uiw/react-markdown-preview/lib/index.js
@@ -1,10 +1,11 @@
 "use strict";
 
 var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault")["default"];
-Object.defineProperty(exports, "__esModule", {
+Object.defineProperty(exports, "esModule", {
   value: true
 });
 var _exportNames = {};
+exports.buildFullPipeline = buildFullPipeline;
 exports["default"] = void 0;
 var _objectSpread2 = _interopRequireDefault(require("@babel/runtime/helpers/objectSpread2"));
 var _toConsumableArray2 = _interopRequireDefault(require("@babel/runtime/helpers/toConsumableArray"));
@@ -30,17 +31,58 @@ Object.keys(_Props).forEach(function (key) {
     }
   });
 });
-var _default = exports["default"] = /*#__PURE__*/_react["default"].forwardRef(function (props, ref) {
-  var _props$disableCopy;
-  var rehypePlugins = [_reservedMeta.reservedMeta, _rehypeRaw["default"], _retrieveMeta.retrieveMeta].concat((0, _toConsumableArray2["default"])(_rehypePlugins.defaultRehypePlugins), [[_rehypeRewrite["default"], {
-    rewrite: (0, _rehypePlugins.rehypeRewriteHandle)((_props$disableCopy = props.disableCopy) !== null && _props$disableCopy !== void 0 ? _props$disableCopy : false, props.rehypeRewrite)
-  }], [_rehypeAttr["default"], {
-    properties: 'attr'
-  }]], (0, _toConsumableArray2["default"])(props.rehypePlugins || []), [[_rehypePrismPlus["default"], {
+// A-07/A-02: 共享插件配置常量（A-07 消除硬编码分散）
+var REHYPE_ATTRS_CONFIG = [_rehypeAttr["default"], { properties: 'attr' }];
+/**
+ * A-02: 纯函数构建 index（完整版） rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ *
+ * 管线顺序（严格，不可随意调整）：
+ *   1. pipeline.prepend    — 用户自定义前缀插件（A-03 OCP）
+ *   2. reservedMeta        — 保留元数据
+ *   3. rehypeRaw           — HTML 解析
+ *   4. retrieveMeta        — 恢复元数据
+ *   5. defaultRehypePlugins — slug/headings/ignore
+ *   6. rehypeRewrite       — AST 重写（代码复制按钮）
+ *   7. rehypeAttrs         — 属性转换（A-07 共享常量）
+ *   8. userPlugins         — 用户自定义插件
+ *   9. rehypePrism         — 代码语法高亮
+ *  10. pipeline.append     — 用户自定义后缀插件（A-03 OCP）
+ */
+function buildFullPipeline(options) {
+  var _options$pipeline, _options$userPlugins, _options$pipeline2;
+  return [].concat((0, _toConsumableArray2["default"])((_options$pipeline = options.pipeline) === null || _options$pipeline === void 0 ? void 0 : _options$pipeline.prepend ?? []), [_reservedMeta.reservedMeta, _rehypeRaw["default"], _retrieveMeta.retrieveMeta], (0, _toConsumableArray2["default"])(_rehypePlugins.defaultRehypePlugins), [[_rehypeRewrite["default"], {
+    rewrite: options.rewriteFn
+  }], REHYPE_ATTRS_CONFIG], (0, _toConsumableArray2["default"])((_options$userPlugins = options.userPlugins) !== null && _options$userPlugins !== void 0 ? _options$userPlugins : []), [[_rehypePrismPlus["default"], {
     ignoreMissing: true
-  }]]);
-  return /*#__PURE__*/(0, _jsxRuntime.jsx)(_preview["default"], (0, _objectSpread2["default"])((0, _objectSpread2["default"])({}, props), {}, {
+  }]], (0, _toConsumableArray2["default"])((_options$pipeline2 = options.pipeline) === null || _options$pipeline2 === void 0 ? void 0 : _options$pipeline2.append ?? []));
+}
+// Q-01/Q-05 性能修复：useMemo 稳定插件数组 + rehypeRewriteHandle 闭包
+var MarkdownPreviewFull = /*#__PURE__*/_react["default"].forwardRef(function (props, ref) {
+  // A-03: 解构已消费的 props，消除幽灵 prop 泄漏到下游 preview.tsx
+  var disableCopy = props.disableCopy;
+  var userRewrite = props.rehypeRewrite;
+  var userPlugins = props.rehypePlugins;
+  var pipeline = props.pipeline;
+  var restProps = (0, _objectSpread2["default"])({}, props);
+  delete restProps.disableCopy;
+  delete restProps.rehypeRewrite;
+  delete restProps.rehypePlugins;
+  delete restProps.pipeline;
+  // Q-05: 稳定 rewrite 闭包
+  var rewriteFn = _react["default"].useMemo(function () {
+    return (0, _rehypePlugins.rehypeRewriteHandle)(disableCopy !== null && disableCopy !== void 0 ? disableCopy : false, userRewrite);
+  }, [disableCopy, userRewrite]);
+  // A-02: 插件链由纯函数 buildFullPipeline 构建（可独立测试）
+  // Q-01: 稳定插件数组
+  var rehypePlugins = _react["default"].useMemo(function () {
+    return buildFullPipeline({ rewriteFn: rewriteFn, userPlugins: userPlugins, pipeline: pipeline });
+  }, [rewriteFn, userPlugins, pipeline]);
+  // A-03: 仅透传未消费的 props
+  return /*#__PURE__*/(0, _jsxRuntime.jsx)(_preview["default"], (0, _objectSpread2["default"])((0, _objectSpread2["default"])({}, restProps), {}, {
     rehypePlugins: rehypePlugins,
     ref: ref
   }));
 });
+MarkdownPreviewFull.displayName = 'MarkdownPreviewFull';
+var _default = exports["default"] = MarkdownPreviewFull;
diff --git a/node_modules/@uiw/react-markdown-preview/lib/nohighlight.d.ts b/node_modules/@uiw/react-markdown-preview/lib/nohighlight.d.ts
index 85d7c47..0b89211 100644
--- a/node_modules/@uiw/react-markdown-preview/lib/nohighlight.d.ts
+++ b/node_modules/@uiw/react-markdown-preview/lib/nohighlight.d.ts
@@ -1,5 +1,15 @@
 import React from 'react';
-import type { MarkdownPreviewProps, MarkdownPreviewRef } from './Props';
-export * from './Props';
-declare const _default: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
-export default _default;
+import type { PluggableList } from 'unified';
+import type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+export type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
+/**
+ * A-02: 纯函数构建 nohighlight rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ */
+export declare function buildNoHighlightPipeline(options: {
+    rewriteFn: (node: any, index?: number | undefined, parent?: any) => void;
+    userPlugins?: PluggableList;
+    pipeline?: PipelineConfig;
+}): PluggableList;
+declare const NoHighlightMarkdown: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
+export default NoHighlightMarkdown;
diff --git a/node_modules/@uiw/react-markdown-preview/lib/nohighlight.js b/node_modules/@uiw/react-markdown-preview/lib/nohighlight.js
index 2900c92..b3a748c 100644
--- a/node_modules/@uiw/react-markdown-preview/lib/nohighlight.js
+++ b/node_modules/@uiw/react-markdown-preview/lib/nohighlight.js
@@ -1,10 +1,11 @@
 "use strict";
 
 var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault")["default"];
-Object.defineProperty(exports, "__esModule", {
+Object.defineProperty(exports, "esModule", {
   value: true
 });
 var _exportNames = {};
+exports.buildNoHighlightPipeline = buildNoHighlightPipeline;
 exports["default"] = void 0;
 var _objectSpread2 = _interopRequireDefault(require("@babel/runtime/helpers/objectSpread2"));
 var _toConsumableArray2 = _interopRequireDefault(require("@babel/runtime/helpers/toConsumableArray"));
@@ -28,15 +29,54 @@ Object.keys(_Props).forEach(function (key) {
     }
   });
 });
-var _default = exports["default"] = /*#__PURE__*/_react["default"].forwardRef(function (props, ref) {
-  var _props$disableCopy;
-  var rehypePlugins = [_reservedMeta.reservedMeta, _retrieveMeta.retrieveMeta].concat((0, _toConsumableArray2["default"])(_rehypePlugins.defaultRehypePlugins), [[_rehypeRewrite["default"], {
-    rewrite: (0, _rehypePlugins.rehypeRewriteHandle)((_props$disableCopy = props.disableCopy) !== null && _props$disableCopy !== void 0 ? _props$disableCopy : false, props.rehypeRewrite)
-  }], [_rehypeAttr["default"], {
-    properties: 'attr'
-  }]], (0, _toConsumableArray2["default"])(props.rehypePlugins || []));
-  return /*#__PURE__*/(0, _jsxRuntime.jsx)(_preview["default"], (0, _objectSpread2["default"])((0, _objectSpread2["default"])({}, props), {}, {
+// A-07/A-02: 共享插件配置常量（A-07 消除硬编码分散）
+var REHYPE_ATTRS_CONFIG = [_rehypeAttr["default"], { properties: 'attr' }];
+/**
+ * A-02: 纯函数构建 nohighlight rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ *
+ * 管线顺序（严格，不可随意调整）：
+ *   1. pipeline.prepend    — 用户自定义前缀插件（A-03 OCP）
+ *   2. reservedMeta        — 保留元数据
+ *   3. retrieveMeta        — 恢复元数据
+ *   4. defaultRehypePlugins — slug/headings/ignore
+ *   5. rehypeRewrite       — AST 重写（代码复制按钮）
+ *   6. rehypeAttrs         — 属性转换（A-07 共享常量）
+ *   7. userPlugins         — 用户自定义插件
+ *   8. pipeline.append     — 用户自定义后缀插件（A-03 OCP）
+ */
+function buildNoHighlightPipeline(options) {
+  var _options$pipeline, _options$userPlugins, _options$pipeline2;
+  return [].concat((0, _toConsumableArray2["default"])((_options$pipeline = options.pipeline) === null || _options$pipeline === void 0 ? void 0 : _options$pipeline.prepend ?? []), [_reservedMeta.reservedMeta, _retrieveMeta.retrieveMeta], (0, _toConsumableArray2["default"])(_rehypePlugins.defaultRehypePlugins), [[_rehypeRewrite["default"], {
+    rewrite: options.rewriteFn
+  }], REHYPE_ATTRS_CONFIG], (0, _toConsumableArray2["default"])((_options$userPlugins = options.userPlugins) !== null && _options$userPlugins !== void 0 ? _options$userPlugins : []), (0, _toConsumableArray2["default"])((_options$pipeline2 = options.pipeline) === null || _options$pipeline2 === void 0 ? void 0 : _options$pipeline2.append ?? []));
+}
+// Q-01/Q-05 性能修复：useMemo 稳定插件数组 + rehypeRewriteHandle 闭包
+var NoHighlightMarkdown = /*#__PURE__*/_react["default"].forwardRef(function (props, ref) {
+  // A-03: 解构已消费的 props，消除幽灵 prop 泄漏到下游 preview.tsx
+  var disableCopy = props.disableCopy;
+  var userRewrite = props.rehypeRewrite;
+  var userPlugins = props.rehypePlugins;
+  var pipeline = props.pipeline;
+  var restProps = (0, _objectSpread2["default"])({}, props);
+  delete restProps.disableCopy;
+  delete restProps.rehypeRewrite;
+  delete restProps.rehypePlugins;
+  delete restProps.pipeline;
+  // Q-05: 稳定 rewrite 闭包
+  var rewriteFn = _react["default"].useMemo(function () {
+    return (0, _rehypePlugins.rehypeRewriteHandle)(disableCopy !== null && disableCopy !== void 0 ? disableCopy : false, userRewrite);
+  }, [disableCopy, userRewrite]);
+  // A-02: 插件链由纯函数 buildNoHighlightPipeline 构建（可独立测试）
+  // Q-01: 稳定插件数组
+  var rehypePlugins = _react["default"].useMemo(function () {
+    return buildNoHighlightPipeline({ rewriteFn: rewriteFn, userPlugins: userPlugins, pipeline: pipeline });
+  }, [rewriteFn, userPlugins, pipeline]);
+  // A-03: 仅透传未消费的 props
+  return /*#__PURE__*/(0, _jsxRuntime.jsx)(_preview["default"], (0, _objectSpread2["default"])((0, _objectSpread2["default"])({}, restProps), {}, {
     rehypePlugins: rehypePlugins,
     ref: ref
   }));
 });
+NoHighlightMarkdown.displayName = 'NoHighlightMarkdown';
+var _default = exports["default"] = NoHighlightMarkdown;
diff --git a/node_modules/@uiw/react-markdown-preview/lib/rehypePlugins.js b/node_modules/@uiw/react-markdown-preview/lib/rehypePlugins.js
index e853c64..a8367fd 100644
--- a/node_modules/@uiw/react-markdown-preview/lib/rehypePlugins.js
+++ b/node_modules/@uiw/react-markdown-preview/lib/rehypePlugins.js
@@ -12,22 +12,43 @@ var _rehypeIgnore = _interopRequireDefault(require("rehype-ignore"));
 var _rehypeRewrite = require("rehype-rewrite");
 var _octiconLink = require("./nodes/octiconLink");
 var _copy = require("./nodes/copy");
+var HEADING_TAGS = new Set(['h1', 'h2', 'h3', 'h4', 'h5', 'h6']);
+var MAX_CODE_LENGTH = 100_000;
+var SAFE_ANCHOR_PROPS = ['ariaHidden', 'href'];
+function escapeHtmlAttr(str) {
+  return str.replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
+}
 var rehypeRewriteHandle = exports.rehypeRewriteHandle = function rehypeRewriteHandle(disableCopy, rewrite) {
   return function (node, index, parent) {
-    if (node.type === 'element' && parent && parent.type === 'root' && /h(1|2|3|4|5|6)/.test(node.tagName)) {
+    if (node.type === 'element' && parent && parent.type === 'root' && HEADING_TAGS.has(node.tagName)) {
       var child = node.children && node.children[0];
-      if (child && child.properties && child.properties.ariaHidden === 'true') {
-        child.properties = (0, _objectSpread2["default"])({
-          "class": 'anchor'
-        }, child.properties);
+      if (child && child.type === 'element' && child.properties && child.properties.ariaHidden === 'true') {
+        var safeProps = { "class": 'anchor' };
+        for (var _i = 0; _i < SAFE_ANCHOR_PROPS.length; _i++) {
+          var key = SAFE_ANCHOR_PROPS[_i];
+          if (child.properties[key] !== undefined) {
+            safeProps[key] = child.properties[key];
+          }
+        }
+        child.properties = safeProps;
         child.children = [_octiconLink.octiconLink];
       }
     }
     if (node.type === 'element' && node.tagName === 'pre' && !disableCopy) {
       var code = (0, _rehypeRewrite.getCodeString)(node.children);
-      node.children.push((0, _copy.copyElement)(code));
+      if (code.length <= MAX_CODE_LENGTH) {
+        node.children.push((0, _copy.copyElement)(escapeHtmlAttr(code)));
+      }
+    }
+    if (rewrite) {
+      try {
+        rewrite(node, index === null ? undefined : index, parent === null ? undefined : parent);
+      } catch (err) {
+        if (typeof console !== 'undefined') {
+          console.warn('[rehypeRewriteHandle] User rewrite callback error:', err);
+        }
+      }
     }
-    rewrite && rewrite(node, index === null ? undefined : index, parent === null ? undefined : parent);
   };
 };
 var defaultRehypePlugins = exports.defaultRehypePlugins = [_rehypeSlug["default"], _rehypeAutolinkHeadings["default"], _rehypeIgnore["default"]];
\ No newline at end of file
diff --git a/node_modules/@uiw/react-markdown-preview/nohighlight.d.ts b/node_modules/@uiw/react-markdown-preview/nohighlight.d.ts
index 5488841..dd818d5 100644
--- a/node_modules/@uiw/react-markdown-preview/nohighlight.d.ts
+++ b/node_modules/@uiw/react-markdown-preview/nohighlight.d.ts
@@ -1,7 +1,17 @@
 declare module '@uiw/react-markdown-preview/nohighlight' {
   import React from 'react';
-  import { MarkdownPreviewProps, MarkdownPreviewRef } from '@uiw/react-markdown-preview/lib/Props';
-  export * from '@uiw/react-markdown-preview/lib/Props';
-  const _default: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
-  export default _default;
+  import type { PluggableList } from 'unified';
+  import { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from '@uiw/react-markdown-preview/lib/Props';
+  export type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from '@uiw/react-markdown-preview/lib/Props';
+  /**
+   * A-02: 纯函数构建 nohighlight rehype 管线。
+   * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+   */
+  export declare function buildNoHighlightPipeline(options: {
+    rewriteFn: (node: any, index?: number | undefined, parent?: any) => void;
+    userPlugins?: PluggableList;
+    pipeline?: PipelineConfig;
+  }): PluggableList;
+  const NoHighlightMarkdown: React.ForwardRefExoticComponent<MarkdownPreviewProps & React.RefAttributes<MarkdownPreviewRef>>;
+  export default NoHighlightMarkdown;
 }
diff --git a/node_modules/@uiw/react-markdown-preview/src/Props.tsx b/node_modules/@uiw/react-markdown-preview/src/Props.tsx
index ac09f1f..3b107fa 100644
--- a/node_modules/@uiw/react-markdown-preview/src/Props.tsx
+++ b/node_modules/@uiw/react-markdown-preview/src/Props.tsx
@@ -2,28 +2,98 @@ import { type Options } from 'react-markdown';
 import { type RehypeRewriteOptions } from 'rehype-rewrite';
 import { type PluggableList } from 'unified';
 
+/**
+ * 管线配置接口（A-03 OCP 修复）。
+ * 允许在默认管线的头部/尾部插入自定义插件，无需修改源码。
+ */
+export interface PipelineConfig {
+  /** 在默认管线之前插入（reservedMeta 之前） */
+  prepend?: PluggableList;
+  /** 在默认管线之后追加（rehypePrism 之后 / nohighlight 最后默认插件之后） */
+  append?: PluggableList;
+}
+
+/**
+ * MarkdownPreview 组件属性。
+ *
+ * ⚠️ 安全说明（SEC-MD-04）：
+ * 本接口继承 react-markdown Options，暴露以下危险属性：
+ *   - `allowElement` — 可允许 `<script>`、`<iframe>` 等危险元素
+ *   - `rehypePlugins` / `remarkPlugins` — 可注入任意 AST 处理插件
+ *   - `urlTransform` — 可修改 URL 转换逻辑，绕过协议过滤
+ * 渲染用户提交内容时，必须配合 allowElement 白名单 + DOMPurify 消毒使用。
+ */
 export interface MarkdownPreviewProps extends Omit<Options, 'children'> {
   prefixCls?: string;
   className?: string;
+  /**
+   * Markdown 源文本。
+   *
+   * ⚠️ 安全警告（SEC-MD-01）：
+   *   - 无长度限制，超长内容可导致 Markdown 解析器 CPU/内存耗尽（DoS）
+   *   - 若渲染层未启用 HTML 过滤，`<script>` 等标签可导致 XSS
+   *   - 恶意链接 `[click](javascript:alert(1))` 可绕过未过滤的协议
+   * 建议对不可信输入进行服务端消毒（DOMPurify），并限制长度上限（建议 1MB）。
+   */
   source?: string;
   disableCopy?: boolean;
   style?: React.CSSProperties;
+  /**
+   * 插件过滤器。
+   *
+   * ⚠️ 安全警告（SEC-MD-03）：
+   * 此回调可过滤/修改插件列表，包括安全过滤插件（如 rehype-sanitize）。
+   * 不当使用可能移除安全插件，导致 XSS 风险。
+   */
   pluginsFilter?: (type: 'rehype' | 'remark', plugin: PluggableList) => PluggableList;
-  wrapperElement?: React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement> & {
+  /**
+   * 容器元素属性（SEC-MD-05 收窄类型）。
+   * 仅允许安全属性，排除 dangerouslySetInnerHTML 和事件处理器。
+   */
+  wrapperElement?: {
+    className?: string;
+    style?: React.CSSProperties;
+    id?: string;
+    role?: string;
     'data-color-mode'?: 'light' | 'dark';
   };
   /**
    * Please use wrapperElement, Will be removed in v5 release.
    * @deprecated
    */
-  warpperElement?: React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement> & {
+  warpperElement?: {
+    className?: string;
+    style?: React.CSSProperties;
+    id?: string;
+    role?: string;
     'data-color-mode'?: 'light' | 'dark';
   };
   onScroll?: (e: React.UIEvent<HTMLDivElement>) => void;
   onMouseOver?: (e: React.MouseEvent<HTMLDivElement>) => void;
+  /**
+   * AST 重写回调。
+   *
+   * ⚠️ 安全警告（SEC-MD-02）：
+   * 此回调运行在安全过滤之后，可以覆盖安全过滤结果。
+   * 可移除安全过滤、注入恶意属性（onclick/onerror）、篡改链接为 javascript: URI。
+   * 仅在完全信任输入源时使用。请勿用于渲染用户提交的 Markdown 内容。
+   */
   rehypeRewrite?: RehypeRewriteOptions['rewrite'];
+  /**
+   * 管线配置（A-03 OCP 修复）。
+   * 允许在默认插件管线的头部/尾部插入自定义插件，无需修改源码。
+   */
+  pipeline?: PipelineConfig;
 }
 
+/**
+ * MarkdownPreview Ref 接口。
+ *
+ * ⚠️ 安全说明（SEC-MD-06）：
+ * 继承全部 Props 属性，通过 ref.current 可读取/修改内部状态。
+ * mdp 为内部 div 的直接 DOM 引用，可进行任意 DOM 操作（绕过 React XSS 防护）。
+ * 建议仅在安全的命令式场景中使用（如 scrollTo）。
+ */
 export interface MarkdownPreviewRef extends MarkdownPreviewProps {
   mdp: React.RefObject<HTMLDivElement>;
 }
diff --git a/node_modules/@uiw/react-markdown-preview/src/common.tsx b/node_modules/@uiw/react-markdown-preview/src/common.tsx
index ae82392..4e4cba8 100644
--- a/node_modules/@uiw/react-markdown-preview/src/common.tsx
+++ b/node_modules/@uiw/react-markdown-preview/src/common.tsx
@@ -1,4 +1,4 @@
-import React from 'react';
+import React, { useMemo } from 'react';
 import rehypePrism from 'rehype-prism-plus/common';
 import type { PluggableList } from 'unified';
 import rehypeRewrite from 'rehype-rewrite';
@@ -8,20 +8,76 @@ import MarkdownPreview from './preview';
 import { reservedMeta } from './plugins/reservedMeta';
 import { retrieveMeta } from './plugins/retrieveMeta';
 import { rehypeRewriteHandle, defaultRehypePlugins } from './rehypePlugins';
-import type { MarkdownPreviewProps, MarkdownPreviewRef } from './Props';
+import type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
 
-export * from './Props';
+export type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
 
-export default React.forwardRef<MarkdownPreviewRef, MarkdownPreviewProps>((props, ref) => {
-  const rehypePlugins: PluggableList = [
+// A-07/A-02: 共享插件配置常量（A-07 消除硬编码分散）
+const REHYPE_ATTRS_CONFIG: PluggableList[number] = [rehypeAttrs, { properties: 'attr' }];
+
+/**
+ * A-02: 纯函数构建 common rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ *
+ * 管线顺序（严格，不可随意调整）：
+ *   1. pipeline.prepend    — 用户自定义前缀插件（A-03 OCP）
+ *   2. reservedMeta        — 保留元数据
+ *   3. rehypeRaw           — HTML 解析
+ *   4. retrieveMeta        — 恢复元数据
+ *   5. defaultRehypePlugins — slug/headings/ignore
+ *   6. rehypeRewrite       — AST 重写（代码复制按钮）
+ *   7. rehypeAttrs         — 属性转换（A-07 共享常量）
+ *   8. userPlugins         — 用户自定义插件
+ *   9. rehypePrism         — 代码语法高亮
+ *  10. pipeline.append     — 用户自定义后缀插件（A-03 OCP）
+ * 注意：preview.tsx 在 skipHtml=false 时也会 push raw，可能导致双重 rehype-raw 执行（Q-04）
+ */
+export function buildCommonPipeline(options: {
+  rewriteFn: ReturnType<typeof rehypeRewriteHandle>;
+  userPlugins?: PluggableList;
+  pipeline?: PipelineConfig;
+}): PluggableList {
+  return [
+    ...(options.pipeline?.prepend ?? []),
     reservedMeta,
     rehypeRaw,
     retrieveMeta,
     ...defaultRehypePlugins,
-    [rehypeRewrite, { rewrite: rehypeRewriteHandle(props.disableCopy ?? false, props.rehypeRewrite) }],
-    [rehypeAttrs, { properties: 'attr' }],
-    ...(props.rehypePlugins || []),
+    [rehypeRewrite, { rewrite: options.rewriteFn }],
+    REHYPE_ATTRS_CONFIG,
+    ...(options.userPlugins ?? []),
     [rehypePrism, { ignoreMissing: true }],
+    ...(options.pipeline?.append ?? []),
   ];
-  return <MarkdownPreview {...props} rehypePlugins={rehypePlugins} ref={ref} />;
+}
+
+// Q-01/Q-05 性能修复：useMemo 稳定插件数组 + rehypeRewriteHandle 闭包
+const MarkdownPreviewHighlighted = React.forwardRef<MarkdownPreviewRef, MarkdownPreviewProps>((props, ref) => {
+  // A-03: 解构已消费的 props，消除幽灵 prop 泄漏到下游 preview.tsx
+  const {
+    disableCopy,
+    rehypeRewrite: userRewrite,
+    rehypePlugins: userPlugins,
+    pipeline,
+    ...restProps
+  } = props;
+
+  // Q-05: 稳定 rewrite 闭包，仅在 disableCopy/userRewrite 变化时重建
+  const rewriteFn = useMemo(
+    () => rehypeRewriteHandle(disableCopy ?? false, userRewrite),
+    [disableCopy, userRewrite],
+  );
+
+  // A-02: 插件链由纯函数 buildCommonPipeline 构建（可独立测试）
+  // Q-01: 稳定插件数组，仅在依赖变化时重建
+  const rehypePlugins: PluggableList = useMemo(
+    () => buildCommonPipeline({ rewriteFn, userPlugins, pipeline }),
+    [rewriteFn, userPlugins, pipeline],
+  );
+
+  // A-03: 仅透传未消费的 props
+  return <MarkdownPreview {...restProps} rehypePlugins={rehypePlugins} ref={ref} />;
 });
+
+MarkdownPreviewHighlighted.displayName = 'MarkdownPreviewHighlighted';
+export default MarkdownPreviewHighlighted;
diff --git a/node_modules/@uiw/react-markdown-preview/src/index.tsx b/node_modules/@uiw/react-markdown-preview/src/index.tsx
index 0063641..ae27a0c 100644
--- a/node_modules/@uiw/react-markdown-preview/src/index.tsx
+++ b/node_modules/@uiw/react-markdown-preview/src/index.tsx
@@ -1,4 +1,4 @@
-import React from 'react';
+import React, { useMemo } from 'react';
 import rehypePrism from 'rehype-prism-plus';
 import type { PluggableList } from 'unified';
 import rehypeRewrite from 'rehype-rewrite';
@@ -8,20 +8,75 @@ import MarkdownPreview from './preview';
 import { reservedMeta } from './plugins/reservedMeta';
 import { retrieveMeta } from './plugins/retrieveMeta';
 import { rehypeRewriteHandle, defaultRehypePlugins } from './rehypePlugins';
-import type { MarkdownPreviewProps, MarkdownPreviewRef } from './Props';
+import type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
 
-export * from './Props';
+export type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
 
-export default React.forwardRef<MarkdownPreviewRef, MarkdownPreviewProps>((props, ref) => {
-  const rehypePlugins: PluggableList = [
+// A-07/A-02: 共享插件配置常量（A-07 消除硬编码分散）
+const REHYPE_ATTRS_CONFIG: PluggableList[number] = [rehypeAttrs, { properties: 'attr' }];
+
+/**
+ * A-02: 纯函数构建 index（完整版） rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ *
+ * 管线顺序（严格，不可随意调整）：
+ *   1. pipeline.prepend    — 用户自定义前缀插件（A-03 OCP）
+ *   2. reservedMeta        — 保留元数据
+ *   3. rehypeRaw           — HTML 解析
+ *   4. retrieveMeta        — 恢复元数据
+ *   5. defaultRehypePlugins — slug/headings/ignore
+ *   6. rehypeRewrite       — AST 重写（代码复制按钮）
+ *   7. rehypeAttrs         — 属性转换（A-07 共享常量）
+ *   8. userPlugins         — 用户自定义插件
+ *   9. rehypePrism         — 代码语法高亮
+ *  10. pipeline.append     — 用户自定义后缀插件（A-03 OCP）
+ */
+export function buildFullPipeline(options: {
+  rewriteFn: ReturnType<typeof rehypeRewriteHandle>;
+  userPlugins?: PluggableList;
+  pipeline?: PipelineConfig;
+}): PluggableList {
+  return [
+    ...(options.pipeline?.prepend ?? []),
     reservedMeta,
     rehypeRaw,
     retrieveMeta,
     ...defaultRehypePlugins,
-    [rehypeRewrite, { rewrite: rehypeRewriteHandle(props.disableCopy ?? false, props.rehypeRewrite) }],
-    [rehypeAttrs, { properties: 'attr' }],
-    ...(props.rehypePlugins || []),
+    [rehypeRewrite, { rewrite: options.rewriteFn }],
+    REHYPE_ATTRS_CONFIG,
+    ...(options.userPlugins ?? []),
     [rehypePrism, { ignoreMissing: true }],
+    ...(options.pipeline?.append ?? []),
   ];
-  return <MarkdownPreview {...props} rehypePlugins={rehypePlugins} ref={ref} />;
+}
+
+// Q-01/Q-05 性能修复：useMemo 稳定插件数组 + rehypeRewriteHandle 闭包
+const MarkdownPreviewFull = React.forwardRef<MarkdownPreviewRef, MarkdownPreviewProps>((props, ref) => {
+  // A-03: 解构已消费的 props，消除幽灵 prop 泄漏到下游 preview.tsx
+  const {
+    disableCopy,
+    rehypeRewrite: userRewrite,
+    rehypePlugins: userPlugins,
+    pipeline,
+    ...restProps
+  } = props;
+
+  // Q-05: 稳定 rewrite 闭包
+  const rewriteFn = useMemo(
+    () => rehypeRewriteHandle(disableCopy ?? false, userRewrite),
+    [disableCopy, userRewrite],
+  );
+
+  // A-02: 插件链由纯函数 buildFullPipeline 构建（可独立测试）
+  // Q-01: 稳定插件数组
+  const rehypePlugins: PluggableList = useMemo(
+    () => buildFullPipeline({ rewriteFn, userPlugins, pipeline }),
+    [rewriteFn, userPlugins, pipeline],
+  );
+
+  // A-03: 仅透传未消费的 props
+  return <MarkdownPreview {...restProps} rehypePlugins={rehypePlugins} ref={ref} />;
 });
+
+MarkdownPreviewFull.displayName = 'MarkdownPreviewFull';
+export default MarkdownPreviewFull;
diff --git a/node_modules/@uiw/react-markdown-preview/src/nohighlight.tsx b/node_modules/@uiw/react-markdown-preview/src/nohighlight.tsx
index d3f895c..b6f2248 100644
--- a/node_modules/@uiw/react-markdown-preview/src/nohighlight.tsx
+++ b/node_modules/@uiw/react-markdown-preview/src/nohighlight.tsx
@@ -1,4 +1,4 @@
-import React from 'react';
+import React, { useMemo } from 'react';
 import MarkdownPreview from './preview';
 import { type PluggableList } from 'unified';
 import rehypeRewrite from 'rehype-rewrite';
@@ -6,18 +6,74 @@ import rehypeAttrs from 'rehype-attr';
 import { reservedMeta } from './plugins/reservedMeta';
 import { retrieveMeta } from './plugins/retrieveMeta';
 import { rehypeRewriteHandle, defaultRehypePlugins } from './rehypePlugins';
-import type { MarkdownPreviewProps, MarkdownPreviewRef } from './Props';
+import type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
 
-export * from './Props';
+export type { MarkdownPreviewProps, MarkdownPreviewRef, PipelineConfig } from './Props';
 
-export default React.forwardRef<MarkdownPreviewRef, MarkdownPreviewProps>((props, ref) => {
-  const rehypePlugins: PluggableList = [
+// A-07/A-02: 共享插件配置常量（A-07 消除硬编码分散）
+const REHYPE_ATTRS_CONFIG: PluggableList[number] = [rehypeAttrs, { properties: 'attr' }];
+
+/**
+ * A-02: 纯函数构建 nohighlight rehype 管线。
+ * 脱离 React 运行时可独立测试，插件链配置与渲染逻辑分离。
+ *
+ * 管线顺序（严格，不可随意调整）：
+ *   1. pipeline.prepend    — 用户自定义前缀插件（A-03 OCP）
+ *   2. reservedMeta        — 保留元数据
+ *   3. retrieveMeta        — 恢复元数据
+ *   4. defaultRehypePlugins — slug/headings/ignore
+ *   5. rehypeRewrite       — AST 重写（代码复制按钮）
+ *   6. rehypeAttrs         — 属性转换（A-07 共享常量）
+ *   7. userPlugins         — 用户自定义插件
+ *   8. pipeline.append     — 用户自定义后缀插件（A-03 OCP）
+ */
+export function buildNoHighlightPipeline(options: {
+  rewriteFn: ReturnType<typeof rehypeRewriteHandle>;
+  userPlugins?: PluggableList;
+  pipeline?: PipelineConfig;
+}): PluggableList {
+  return [
+    ...(options.pipeline?.prepend ?? []),
     reservedMeta,
     retrieveMeta,
     ...defaultRehypePlugins,
-    [rehypeRewrite, { rewrite: rehypeRewriteHandle(props.disableCopy ?? false, props.rehypeRewrite) }],
-    [rehypeAttrs, { properties: 'attr' }],
-    ...(props.rehypePlugins || []),
+    [rehypeRewrite, { rewrite: options.rewriteFn }],
+    REHYPE_ATTRS_CONFIG,
+    ...(options.userPlugins ?? []),
+    ...(options.pipeline?.append ?? []),
   ];
-  return <MarkdownPreview {...props} rehypePlugins={rehypePlugins} ref={ref} />;
+}
+
+// Q-01/Q-05 性能修复：useMemo 稳定插件数组 + rehypeRewriteHandle 闭包
+const NoHighlightMarkdown = React.forwardRef<MarkdownPreviewRef, MarkdownPreviewProps>((props, ref) => {
+  // A-03: 解构已消费的 props，消除幽灵 prop 泄漏到下游 preview.tsx
+  // disableCopy/rehypeRewrite → 构建 rewrite 闭包（已消费）
+  // rehypePlugins/pipeline    → 构建插件链（已消费）
+  // 其余 props 通过 restProps 透传给 MarkdownPreview
+  const {
+    disableCopy,
+    rehypeRewrite: userRewrite,
+    rehypePlugins: userPlugins,
+    pipeline,
+    ...restProps
+  } = props;
+
+  // Q-05: 稳定 rewrite 闭包，仅在 disableCopy/userRewrite 变化时重建
+  const rewriteFn = useMemo(
+    () => rehypeRewriteHandle(disableCopy ?? false, userRewrite),
+    [disableCopy, userRewrite],
+  );
+
+  // A-02: 插件链由纯函数 buildNoHighlightPipeline 构建（可独立测试）
+  // Q-01: 稳定插件数组，仅在依赖变化时重建
+  const rehypePlugins: PluggableList = useMemo(
+    () => buildNoHighlightPipeline({ rewriteFn, userPlugins, pipeline }),
+    [rewriteFn, userPlugins, pipeline],
+  );
+
+  // A-03: 仅透传未消费的 props，不再泄漏 disableCopy/rehypeRewrite/rehypePlugins/pipeline
+  return <MarkdownPreview {...restProps} rehypePlugins={rehypePlugins} ref={ref} />;
 });
+
+NoHighlightMarkdown.displayName = 'NoHighlightMarkdown';
+export default NoHighlightMarkdown;
diff --git a/node_modules/@uiw/react-markdown-preview/src/rehypePlugins.tsx b/node_modules/@uiw/react-markdown-preview/src/rehypePlugins.tsx
index 680ce6a..fb7cbe2 100644
--- a/node_modules/@uiw/react-markdown-preview/src/rehypePlugins.tsx
+++ b/node_modules/@uiw/react-markdown-preview/src/rehypePlugins.tsx
@@ -7,21 +7,48 @@ import type { Root, Element, RootContent } from 'hast';
 import { octiconLink } from './nodes/octiconLink';
 import { copyElement } from './nodes/copy';
 
+const HEADING_TAGS = new Set(['h1', 'h2', 'h3', 'h4', 'h5', 'h6']);
+const MAX_CODE_LENGTH = 100_000; // 代码块复制按钮上限（与 useCopied MAX_COPY_LENGTH 一致）
+const SAFE_ANCHOR_PROPS = ['ariaHidden', 'href'] as const;
+
+function escapeHtmlAttr(str: string): string {
+  return str.replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
+}
+
 export const rehypeRewriteHandle =
   (disableCopy: boolean, rewrite?: RehypeRewriteOptions['rewrite']) =>
   (node: Root | RootContent, index: number | null, parent: Root | Element | null) => {
-    if (node.type === 'element' && parent && parent.type === 'root' && /h(1|2|3|4|5|6)/.test(node.tagName)) {
-      const child = node.children && (node.children[0] as Element);
-      if (child && child.properties && child.properties.ariaHidden === 'true') {
-        child.properties = { class: 'anchor', ...child.properties };
+    // 标题锚点图标注入（SEC-02: 属性白名单过滤，仅保留安全属性）
+    if (node.type === 'element' && parent?.type === 'root' && HEADING_TAGS.has(node.tagName)) {
+      const child = node.children?.[0];
+      if (child?.type === 'element' && child.properties?.ariaHidden === 'true') {
+        const safeProps: Record<string, unknown> = { class: 'anchor' };
+        for (const key of SAFE_ANCHOR_PROPS) {
+          if (child.properties[key] !== undefined) {
+            safeProps[key] = child.properties[key];
+          }
+        }
+        child.properties = safeProps;
         child.children = [octiconLink];
       }
     }
+    // 代码块复制按钮注入（SEC-03: 长度限制防止 DOM 膨胀 DoS）
     if (node.type === 'element' && node.tagName === 'pre' && !disableCopy) {
       const code = getCodeString(node.children);
-      node.children.push(copyElement(code));
+      if (code.length <= MAX_CODE_LENGTH) {
+        node.children.push(copyElement(escapeHtmlAttr(code)));
+      }
+    }
+    // 用户自定义 rewrite 委托（带错误边界保护）
+    if (rewrite) {
+      try {
+        rewrite(node, index ?? undefined, parent ?? undefined);
+      } catch (err) {
+        if (typeof console !== 'undefined') {
+          console.warn('[rehypeRewriteHandle] User rewrite callback error:', err);
+        }
+      }
     }
-    rewrite && rewrite(node, index === null ? undefined : index, parent === null ? undefined : parent);
   };
 
 export const defaultRehypePlugins: PluggableList = [slug, headings, rehypeIgnore];
