diff --git a/node_modules/@uiw/react-md-editor/esm/Context.d.ts b/node_modules/@uiw/react-md-editor/esm/Context.d.ts
index ede0897..7efd760 100644
--- a/node_modules/@uiw/react-md-editor/esm/Context.d.ts
+++ b/node_modules/@uiw/react-md-editor/esm/Context.d.ts
@@ -24,31 +24,11 @@ export interface ContextStore {
     scrollTopPreview?: number;
     tabSize?: number;
     defaultTabEnable?: boolean;
-    [key: string]: any;
 }
-export type ExecuteCommandState = Pick<ContextStore, 'fullscreen' | 'preview' | 'highlightEnable'>;
-export declare function reducer(state: ContextStore, action: ContextStore): {
-    [x: string]: any;
-    components?: MDEditorProps["components"];
-    commands?: ICommand<string>[];
-    extraCommands?: ICommand<string>[];
-    markdown?: string;
-    preview?: PreviewType;
-    height?: React.CSSProperties["height"];
+export interface ExecuteCommandState {
     fullscreen?: boolean;
+    preview?: PreviewType;
     highlightEnable?: boolean;
-    autoFocus?: boolean;
-    autoFocusEnd?: boolean;
-    textarea?: HTMLTextAreaElement;
-    commandOrchestrator?: TextAreaCommandOrchestrator;
-    textareaWarp?: HTMLDivElement;
-    textareaPre?: HTMLPreElement;
-    container?: HTMLDivElement | null;
-    dispatch?: React.Dispatch<ContextStore>;
-    barPopup?: Record<string, boolean>;
-    scrollTop?: number;
-    scrollTopPreview?: number;
-    tabSize?: number;
-    defaultTabEnable?: boolean;
-};
+}
+export declare function reducer(state: ContextStore, action: ContextStore): ContextStore;
 export declare const EditorContext: React.Context<ContextStore>;
diff --git a/node_modules/@uiw/react-md-editor/esm/Context.js b/node_modules/@uiw/react-md-editor/esm/Context.js
index 18a2a64..324f28b 100644
--- a/node_modules/@uiw/react-md-editor/esm/Context.js
+++ b/node_modules/@uiw/react-md-editor/esm/Context.js
@@ -1,8 +1,38 @@
 import _extends from "@babel/runtime/helpers/extends";
 import React from 'react';
+
+// SEC-CTX-04: Reducer whitelist — only known keys are merged into state
+var REDUCER_ALLOWED_KEYS = new Set([
+  'components', 'commands', 'extraCommands', 'markdown', 'preview',
+  'height', 'fullscreen', 'highlightEnable', 'autoFocus', 'autoFocusEnd',
+  'textarea', 'commandOrchestrator', 'textareaWarp', 'textareaPre',
+  'container', 'dispatch', 'barPopup', 'scrollTop', 'scrollTopPreview',
+  'tabSize', 'defaultTabEnable'
+]);
+
 export function reducer(state, action) {
-  return _extends({}, state, action);
+  if (!action || typeof action !== 'object') return state;
+  var filtered = {};
+  var keys = Object.keys(action);
+  for (var i = 0; i < keys.length; i++) {
+    if (REDUCER_ALLOWED_KEYS.has(keys[i])) {
+      filtered[keys[i]] = action[keys[i]];
+    }
+  }
+  return _extends({}, state, filtered);
 }
+
+// SEC-CTX-05: Complete default values for ContextStore
 export var EditorContext = /*#__PURE__*/React.createContext({
-  markdown: ''
+  markdown: '',
+  preview: 'live',
+  fullscreen: false,
+  highlightEnable: true,
+  autoFocus: false,
+  autoFocusEnd: false,
+  barPopup: {},
+  scrollTop: 0,
+  scrollTopPreview: 0,
+  tabSize: 2,
+  defaultTabEnable: true
 });
\ No newline at end of file
diff --git a/node_modules/@uiw/react-md-editor/esm/Editor.factory.js b/node_modules/@uiw/react-md-editor/esm/Editor.factory.js
index 2c80751..dfa930a 100644
--- a/node_modules/@uiw/react-md-editor/esm/Editor.factory.js
+++ b/node_modules/@uiw/react-md-editor/esm/Editor.factory.js
@@ -1,7 +1,7 @@
 import _extends from "@babel/runtime/helpers/extends";
 import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
 var _excluded = ["prefixCls", "className", "value", "commands", "commandsFilter", "direction", "extraCommands", "height", "enableScroll", "visibleDragbar", "highlightEnable", "preview", "fullscreen", "overflow", "previewOptions", "textareaProps", "maxHeight", "minHeight", "autoFocus", "autoFocusEnd", "tabSize", "defaultTabEnable", "onChange", "onStatistics", "onHeightChange", "hideToolbar", "toolbarBottom", "components", "renderTextarea"];
-import React, { useEffect, useReducer, useMemo, useRef, useImperativeHandle } from 'react';
+import React, { useCallback, useEffect, useReducer, useMemo, useRef, useImperativeHandle } from 'react';
 import { ToolbarVisibility } from "./components/Toolbar/index.js";
 import DragBar from "./components/DragBar/index.js";
 import { getCommands, getExtraCommands, TextAreaCommandOrchestrator } from "./commands/index.js";
@@ -11,10 +11,11 @@ function setGroupPopFalse(data) {
   if (data === void 0) {
     data = {};
   }
+  var result = {};
   Object.keys(data).forEach(keyname => {
-    data[keyname] = false;
+    result[keyname] = false;
   });
-  return data;
+  return result;
 }
 export function createMDEditor(options) {
   var {
@@ -91,76 +92,88 @@ export function createMDEditor(options) {
       stateInit.markdown = propsValue || '';
       stateInit.barPopup = {};
       if (dispatch) {
-        dispatch(_extends({}, state, stateInit));
+        dispatch(stateInit);
       }
       // eslint-disable-next-line react-hooks/exhaustive-deps
     }, []);
     var cls = [className, 'wmde-markdown-var', direction ? prefixCls + "-" + direction : null, prefixCls, state.preview ? prefixCls + "-show-" + state.preview : null, state.fullscreen ? prefixCls + "-fullscreen" : null].filter(Boolean).join(' ').trim();
-    useMemo(() => propsValue !== state.markdown && dispatch({
-      markdown: propsValue || ''
-    }), [propsValue, state.markdown]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    useMemo(() => previewType !== state.preview && dispatch({
-      preview: previewType
-    }), [previewType]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    useMemo(() => tabSize !== state.tabSize && dispatch({
-      tabSize
-    }), [tabSize]);
-    useMemo(() => highlightEnable !== state.highlightEnable && dispatch({
-      highlightEnable
-    }),
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    [highlightEnable]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    useMemo(() => autoFocus !== state.autoFocus && dispatch({
-      autoFocus: autoFocus
-    }), [autoFocus]);
-    useMemo(() => autoFocusEnd !== state.autoFocusEnd && dispatch({
-      autoFocusEnd: autoFocusEnd
-    }), [autoFocusEnd]);
-    useMemo(() => fullscreen !== state.fullscreen && dispatch({
-      fullscreen: fullscreen
-    }),
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    [fullscreen]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    useMemo(() => height !== state.height && dispatch({
-      height: height
-    }), [height]);
-    useMemo(() => height !== state.height && onHeightChange && onHeightChange(state.height, height, state), [height, onHeightChange, state]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    useMemo(() => commands !== state.commands && dispatch({
-      commands: cmds
-    }), [props.commands]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    useMemo(() => extraCommands !== state.extraCommands && dispatch({
-      extraCommands: extraCmds
-    }), [props.extraCommands]);
+    useEffect(() => {
+      if (propsValue !== state.markdown) {
+        dispatch({ markdown: propsValue || '' });
+      }
+    }, [propsValue]);
+    useEffect(() => {
+      if (previewType !== state.preview) {
+        dispatch({ preview: previewType });
+      }
+    }, [previewType]);
+    useEffect(() => {
+      if (tabSize !== state.tabSize) {
+        dispatch({ tabSize });
+      }
+    }, [tabSize]);
+    useEffect(() => {
+      if (highlightEnable !== state.highlightEnable) {
+        dispatch({ highlightEnable });
+      }
+    }, [highlightEnable]);
+    useEffect(() => {
+      if (autoFocus !== state.autoFocus) {
+        dispatch({ autoFocus: autoFocus });
+      }
+    }, [autoFocus]);
+    useEffect(() => {
+      if (autoFocusEnd !== state.autoFocusEnd) {
+        dispatch({ autoFocusEnd: autoFocusEnd });
+      }
+    }, [autoFocusEnd]);
+    useEffect(() => {
+      if (fullscreen !== state.fullscreen) {
+        dispatch({ fullscreen: fullscreen });
+      }
+    }, [fullscreen]);
+    useEffect(() => {
+      if (height !== state.height) {
+        var oldHeight = state.height;
+        dispatch({ height: height });
+        if (onHeightChange) {
+          onHeightChange(oldHeight, height, state);
+        }
+      }
+    }, [height]);
+    useEffect(() => {
+      if (commands !== state.commands) {
+        dispatch({ commands: cmds });
+      }
+    }, [props.commands]);
+    useEffect(() => {
+      if (extraCommands !== state.extraCommands) {
+        dispatch({ extraCommands: extraCmds });
+      }
+    }, [props.extraCommands]);
     var textareaDomRef = useRef();
     var active = useRef('preview');
-    var initScroll = useRef(false);
-    useMemo(() => {
+    useEffect(() => {
       textareaDomRef.current = state.textareaWarp;
-      if (state.textareaWarp) {
-        state.textareaWarp.addEventListener('mouseover', () => {
-          active.current = 'text';
-        });
-        state.textareaWarp.addEventListener('mouseleave', () => {
-          active.current = 'preview';
-        });
-      }
+      if (!state.textareaWarp) return;
+      var onMouseOver = () => { active.current = 'text'; };
+      var onMouseLeave = () => { active.current = 'preview'; };
+      state.textareaWarp.addEventListener('mouseover', onMouseOver);
+      state.textareaWarp.addEventListener('mouseleave', onMouseLeave);
+      return () => {
+        state.textareaWarp.removeEventListener('mouseover', onMouseOver);
+        state.textareaWarp.removeEventListener('mouseleave', onMouseLeave);
+      };
     }, [state.textareaWarp]);
     var handleScroll = (e, type) => {
       if (!enableScrollRef.current) return;
       var textareaDom = textareaDomRef.current;
-      var previewDom = previewRef.current ? previewRef.current : undefined;
-      if (!initScroll.current) {
-        active.current = type;
-        initScroll.current = true;
-      }
+      var previewDom = previewRef.current;
       if (textareaDom && previewDom) {
-        var scale = (textareaDom.scrollHeight - textareaDom.offsetHeight) / (previewDom.scrollHeight - previewDom.offsetHeight);
+        var numerator = textareaDom.scrollHeight - textareaDom.offsetHeight;
+        var denominator = previewDom.scrollHeight - previewDom.offsetHeight;
+        if (denominator === 0 || numerator === 0) return;
+        var scale = numerator / denominator;
         if (e.target === textareaDom && active.current === 'text') {
           previewDom.scrollTop = textareaDom.scrollTop / scale;
         }
@@ -178,20 +191,23 @@ export function createMDEditor(options) {
         });
       }
     };
-    var previewClassName = prefixCls + "-preview " + (previewOptions.className || '');
     var handlePreviewScroll = e => handleScroll(e, 'preview');
-    var mdPreview = useMemo(() => /*#__PURE__*/_jsx("div", {
-      ref: previewRef,
-      className: previewClassName,
-      children: /*#__PURE__*/_jsx(PreviewComponent, _extends({}, previewOptions, {
-        onScroll: handlePreviewScroll,
-        source: state.markdown || ''
-      }))
-    }), [previewClassName, previewOptions, state.markdown]);
+    var mdPreview = useMemo(() => {
+      var previewClassName = prefixCls + "-preview " + (previewOptions.className || '');
+      return /*#__PURE__*/_jsx("div", {
+        ref: previewRef,
+        className: previewClassName,
+        children: /*#__PURE__*/_jsx(PreviewComponent, _extends({}, previewOptions, {
+          onScroll: handlePreviewScroll,
+          source: state.markdown || ''
+        }))
+      });
+    }, [prefixCls, previewOptions, state.markdown]);
     var preview = (components == null ? void 0 : components.preview) && (components == null ? void 0 : components.preview(state.markdown || '', state, dispatch));
     if (preview && /*#__PURE__*/React.isValidElement(preview)) {
+      var customPreviewClassName = prefixCls + "-preview " + (previewOptions.className || '');
       mdPreview = /*#__PURE__*/_jsx("div", {
-        className: previewClassName,
+        className: customPreviewClassName,
         ref: previewRef,
         onScroll: handlePreviewScroll,
         children: preview
@@ -206,7 +222,7 @@ export function createMDEditor(options) {
     var dragBarChange = newHeight => dispatch({
       height: newHeight
     });
-    var changeHandle = evn => {
+    var changeHandle = useCallback(evn => {
       onChange && onChange(evn.target.value, evn, state);
       if (textareaProps && textareaProps.onChange) {
         textareaProps.onChange(evn);
@@ -219,7 +235,7 @@ export function createMDEditor(options) {
           length: evn.target.value.length
         }));
       }
-    };
+    }, [onChange, state, textareaProps, onStatistics]);
     return /*#__PURE__*/_jsx(EditorContext.Provider, {
       value: _extends({}, state, {
         dispatch
diff --git a/node_modules/@uiw/react-md-editor/esm/commands/preview.js b/node_modules/@uiw/react-md-editor/esm/commands/preview.js
index 114d987..2b16b6a 100644
--- a/node_modules/@uiw/react-md-editor/esm/commands/preview.js
+++ b/node_modules/@uiw/react-md-editor/esm/commands/preview.js
@@ -13,6 +13,8 @@ export var codePreview = {
     width: "12",
     height: "12",
     viewBox: "0 0 520 520",
+    role: "img",
+    "aria-hidden": "true",
     children: [/*#__PURE__*/_jsx("polygon", {
       fill: "currentColor",
       points: "0 71.293 0 122 38.023 123 38.023 398 0 397 0 449.707 91.023 450.413 91.023 72.293"
@@ -21,9 +23,9 @@ export var codePreview = {
       points: "148.023 72.293 520 71.293 520 122 200.023 124 200.023 397 520 396 520 449.707 148.023 450.413"
     })]
   }),
-  execute: (state, api, dispatch, executeCommandState, shortcuts) => {
-    api.textArea.focus();
-    if (shortcuts && dispatch && executeCommandState) {
+  execute: (_state, api, dispatch, _executeCommandState, shortcuts) => {
+    api.textArea?.focus();
+    if (shortcuts && dispatch && _executeCommandState) {
       dispatch({
         preview: 'preview'
       });
@@ -43,6 +45,8 @@ export var codeEdit = {
     width: "12",
     height: "12",
     viewBox: "0 0 520 520",
+    role: "img",
+    "aria-hidden": "true",
     children: [/*#__PURE__*/_jsx("polygon", {
       fill: "currentColor",
       points: "0 71.293 0 122 319 122 319 397 0 397 0 449.707 372 449.413 372 71.293"
@@ -51,9 +55,9 @@ export var codeEdit = {
       points: "429 71.293 520 71.293 520 122 481 123 481 396 520 396 520 449.707 429 449.413"
     })]
   }),
-  execute: (state, api, dispatch, executeCommandState, shortcuts) => {
-    api.textArea.focus();
-    if (shortcuts && dispatch && executeCommandState) {
+  execute: (_state, api, dispatch, _executeCommandState, shortcuts) => {
+    api.textArea?.focus();
+    if (shortcuts && dispatch && _executeCommandState) {
       dispatch({
         preview: 'edit'
       });
@@ -73,6 +77,8 @@ export var codeLive = {
     width: "12",
     height: "12",
     viewBox: "0 0 520 520",
+    role: "img",
+    "aria-hidden": "true",
     children: [/*#__PURE__*/_jsx("polygon", {
       fill: "currentColor",
       points: "0 71.293 0 122 179 122 179 397 0 397 0 449.707 232 449.413 232 71.293"
@@ -81,9 +87,9 @@ export var codeLive = {
       points: "289 71.293 520 71.293 520 122 341 123 341 396 520 396 520 449.707 289 449.413"
     })]
   }),
-  execute: (state, api, dispatch, executeCommandState, shortcuts) => {
-    api.textArea.focus();
-    if (shortcuts && dispatch && executeCommandState) {
+  execute: (_state, api, dispatch, _executeCommandState, shortcuts) => {
+    api.textArea?.focus();
+    if (shortcuts && dispatch && _executeCommandState) {
       dispatch({
         preview: 'live'
       });
diff --git a/node_modules/@uiw/react-md-editor/lib/Context.d.ts b/node_modules/@uiw/react-md-editor/lib/Context.d.ts
index ede0897..7efd760 100644
--- a/node_modules/@uiw/react-md-editor/lib/Context.d.ts
+++ b/node_modules/@uiw/react-md-editor/lib/Context.d.ts
@@ -24,31 +24,11 @@ export interface ContextStore {
     scrollTopPreview?: number;
     tabSize?: number;
     defaultTabEnable?: boolean;
-    [key: string]: any;
 }
-export type ExecuteCommandState = Pick<ContextStore, 'fullscreen' | 'preview' | 'highlightEnable'>;
-export declare function reducer(state: ContextStore, action: ContextStore): {
-    [x: string]: any;
-    components?: MDEditorProps["components"];
-    commands?: ICommand<string>[];
-    extraCommands?: ICommand<string>[];
-    markdown?: string;
-    preview?: PreviewType;
-    height?: React.CSSProperties["height"];
+export interface ExecuteCommandState {
     fullscreen?: boolean;
+    preview?: PreviewType;
     highlightEnable?: boolean;
-    autoFocus?: boolean;
-    autoFocusEnd?: boolean;
-    textarea?: HTMLTextAreaElement;
-    commandOrchestrator?: TextAreaCommandOrchestrator;
-    textareaWarp?: HTMLDivElement;
-    textareaPre?: HTMLPreElement;
-    container?: HTMLDivElement | null;
-    dispatch?: React.Dispatch<ContextStore>;
-    barPopup?: Record<string, boolean>;
-    scrollTop?: number;
-    scrollTopPreview?: number;
-    tabSize?: number;
-    defaultTabEnable?: boolean;
-};
+}
+export declare function reducer(state: ContextStore, action: ContextStore): ContextStore;
 export declare const EditorContext: React.Context<ContextStore>;
diff --git a/node_modules/@uiw/react-md-editor/lib/Context.js b/node_modules/@uiw/react-md-editor/lib/Context.js
index 375c783..eb9c3b8 100644
--- a/node_modules/@uiw/react-md-editor/lib/Context.js
+++ b/node_modules/@uiw/react-md-editor/lib/Context.js
@@ -8,9 +8,39 @@ exports.EditorContext = void 0;
 exports.reducer = reducer;
 var _objectSpread2 = _interopRequireDefault(require("@babel/runtime/helpers/objectSpread2"));
 var _react = _interopRequireDefault(require("react"));
+
+// SEC-CTX-04: Reducer whitelist — only known keys are merged into state
+var REDUCER_ALLOWED_KEYS = new Set([
+  'components', 'commands', 'extraCommands', 'markdown', 'preview',
+  'height', 'fullscreen', 'highlightEnable', 'autoFocus', 'autoFocusEnd',
+  'textarea', 'commandOrchestrator', 'textareaWarp', 'textareaPre',
+  'container', 'dispatch', 'barPopup', 'scrollTop', 'scrollTopPreview',
+  'tabSize', 'defaultTabEnable'
+]);
+
 function reducer(state, action) {
-  return (0, _objectSpread2["default"])((0, _objectSpread2["default"])({}, state), action);
+  if (!action || typeof action !== 'object') return state;
+  var filtered = {};
+  var keys = Object.keys(action);
+  for (var i = 0; i < keys.length; i++) {
+    if (REDUCER_ALLOWED_KEYS.has(keys[i])) {
+      filtered[keys[i]] = action[keys[i]];
+    }
+  }
+  return (0, _objectSpread2["default"])((0, _objectSpread2["default"])({}, state), filtered);
 }
+
+// SEC-CTX-05: Complete default values for ContextStore
 var EditorContext = exports.EditorContext = /*#__PURE__*/_react["default"].createContext({
-  markdown: ''
+  markdown: '',
+  preview: 'live',
+  fullscreen: false,
+  highlightEnable: true,
+  autoFocus: false,
+  autoFocusEnd: false,
+  barPopup: {},
+  scrollTop: 0,
+  scrollTopPreview: 0,
+  tabSize: 2,
+  defaultTabEnable: true
 });
\ No newline at end of file
diff --git a/node_modules/@uiw/react-md-editor/lib/Editor.factory.js b/node_modules/@uiw/react-md-editor/lib/Editor.factory.js
index d1c73c0..4c95ccd 100644
--- a/node_modules/@uiw/react-md-editor/lib/Editor.factory.js
+++ b/node_modules/@uiw/react-md-editor/lib/Editor.factory.js
@@ -18,10 +18,11 @@ var _jsxRuntime = require("react/jsx-runtime");
 var _excluded = ["prefixCls", "className", "value", "commands", "commandsFilter", "direction", "extraCommands", "height", "enableScroll", "visibleDragbar", "highlightEnable", "preview", "fullscreen", "overflow", "previewOptions", "textareaProps", "maxHeight", "minHeight", "autoFocus", "autoFocusEnd", "tabSize", "defaultTabEnable", "onChange", "onStatistics", "onHeightChange", "hideToolbar", "toolbarBottom", "components", "renderTextarea"];
 function setGroupPopFalse() {
   var data = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
+  var result = {};
   Object.keys(data).forEach(function (keyname) {
-    data[keyname] = false;
+    result[keyname] = false;
   });
-  return data;
+  return result;
 }
 function createMDEditor(options) {
   var MarkdownPreview = options.MarkdownPreview,
@@ -122,98 +123,88 @@ function createMDEditor(options) {
       stateInit.markdown = propsValue || '';
       stateInit.barPopup = {};
       if (dispatch) {
-        dispatch((0, _objectSpread2["default"])((0, _objectSpread2["default"])({}, state), stateInit));
+        dispatch(stateInit);
       }
       // eslint-disable-next-line react-hooks/exhaustive-deps
     }, []);
     var cls = [className, 'wmde-markdown-var', direction ? "".concat(prefixCls, "-").concat(direction) : null, prefixCls, state.preview ? "".concat(prefixCls, "-show-").concat(state.preview) : null, state.fullscreen ? "".concat(prefixCls, "-fullscreen") : null].filter(Boolean).join(' ').trim();
-    (0, _react.useMemo)(function () {
-      return propsValue !== state.markdown && dispatch({
-        markdown: propsValue || ''
-      });
-    }, [propsValue, state.markdown]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    (0, _react.useMemo)(function () {
-      return previewType !== state.preview && dispatch({
-        preview: previewType
-      });
+    (0, _react.useEffect)(function () {
+      if (propsValue !== state.markdown) {
+        dispatch({ markdown: propsValue || '' });
+      }
+    }, [propsValue]);
+    (0, _react.useEffect)(function () {
+      if (previewType !== state.preview) {
+        dispatch({ preview: previewType });
+      }
     }, [previewType]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    (0, _react.useMemo)(function () {
-      return tabSize !== state.tabSize && dispatch({
-        tabSize: tabSize
-      });
+    (0, _react.useEffect)(function () {
+      if (tabSize !== state.tabSize) {
+        dispatch({ tabSize: tabSize });
+      }
     }, [tabSize]);
-    (0, _react.useMemo)(function () {
-      return highlightEnable !== state.highlightEnable && dispatch({
-        highlightEnable: highlightEnable
-      });
-    },
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    [highlightEnable]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    (0, _react.useMemo)(function () {
-      return autoFocus !== state.autoFocus && dispatch({
-        autoFocus: autoFocus
-      });
+    (0, _react.useEffect)(function () {
+      if (highlightEnable !== state.highlightEnable) {
+        dispatch({ highlightEnable: highlightEnable });
+      }
+    }, [highlightEnable]);
+    (0, _react.useEffect)(function () {
+      if (autoFocus !== state.autoFocus) {
+        dispatch({ autoFocus: autoFocus });
+      }
     }, [autoFocus]);
-    (0, _react.useMemo)(function () {
-      return autoFocusEnd !== state.autoFocusEnd && dispatch({
-        autoFocusEnd: autoFocusEnd
-      });
+    (0, _react.useEffect)(function () {
+      if (autoFocusEnd !== state.autoFocusEnd) {
+        dispatch({ autoFocusEnd: autoFocusEnd });
+      }
     }, [autoFocusEnd]);
-    (0, _react.useMemo)(function () {
-      return fullscreen !== state.fullscreen && dispatch({
-        fullscreen: fullscreen
-      });
-    },
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    [fullscreen]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    (0, _react.useMemo)(function () {
-      return height !== state.height && dispatch({
-        height: height
-      });
+    (0, _react.useEffect)(function () {
+      if (fullscreen !== state.fullscreen) {
+        dispatch({ fullscreen: fullscreen });
+      }
+    }, [fullscreen]);
+    (0, _react.useEffect)(function () {
+      if (height !== state.height) {
+        var oldHeight = state.height;
+        dispatch({ height: height });
+        if (onHeightChange) {
+          onHeightChange(oldHeight, height, state);
+        }
+      }
     }, [height]);
-    (0, _react.useMemo)(function () {
-      return height !== state.height && onHeightChange && onHeightChange(state.height, height, state);
-    }, [height, onHeightChange, state]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    (0, _react.useMemo)(function () {
-      return commands !== state.commands && dispatch({
-        commands: cmds
-      });
+    (0, _react.useEffect)(function () {
+      if (commands !== state.commands) {
+        dispatch({ commands: cmds });
+      }
     }, [props.commands]);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-    (0, _react.useMemo)(function () {
-      return extraCommands !== state.extraCommands && dispatch({
-        extraCommands: extraCmds
-      });
+    (0, _react.useEffect)(function () {
+      if (extraCommands !== state.extraCommands) {
+        dispatch({ extraCommands: extraCmds });
+      }
     }, [props.extraCommands]);
     var textareaDomRef = (0, _react.useRef)();
     var active = (0, _react.useRef)('preview');
-    var initScroll = (0, _react.useRef)(false);
-    (0, _react.useMemo)(function () {
+    (0, _react.useEffect)(function () {
       textareaDomRef.current = state.textareaWarp;
-      if (state.textareaWarp) {
-        state.textareaWarp.addEventListener('mouseover', function () {
-          active.current = 'text';
-        });
-        state.textareaWarp.addEventListener('mouseleave', function () {
-          active.current = 'preview';
-        });
-      }
+      if (!state.textareaWarp) return;
+      var onMouseOver = function onMouseOver() { active.current = 'text'; };
+      var onMouseLeave = function onMouseLeave() { active.current = 'preview'; };
+      state.textareaWarp.addEventListener('mouseover', onMouseOver);
+      state.textareaWarp.addEventListener('mouseleave', onMouseLeave);
+      return function () {
+        state.textareaWarp.removeEventListener('mouseover', onMouseOver);
+        state.textareaWarp.removeEventListener('mouseleave', onMouseLeave);
+      };
     }, [state.textareaWarp]);
     var handleScroll = function handleScroll(e, type) {
       if (!enableScrollRef.current) return;
       var textareaDom = textareaDomRef.current;
-      var previewDom = previewRef.current ? previewRef.current : undefined;
-      if (!initScroll.current) {
-        active.current = type;
-        initScroll.current = true;
-      }
+      var previewDom = previewRef.current;
       if (textareaDom && previewDom) {
-        var scale = (textareaDom.scrollHeight - textareaDom.offsetHeight) / (previewDom.scrollHeight - previewDom.offsetHeight);
+        var numerator = textareaDom.scrollHeight - textareaDom.offsetHeight;
+        var denominator = previewDom.scrollHeight - previewDom.offsetHeight;
+        if (denominator === 0 || numerator === 0) return;
+        var scale = numerator / denominator;
         if (e.target === textareaDom && active.current === 'text') {
           previewDom.scrollTop = textareaDom.scrollTop / scale;
         }
@@ -231,11 +222,11 @@ function createMDEditor(options) {
         });
       }
     };
-    var previewClassName = "".concat(prefixCls, "-preview ").concat(previewOptions.className || '');
     var handlePreviewScroll = function handlePreviewScroll(e) {
       return handleScroll(e, 'preview');
     };
     var mdPreview = (0, _react.useMemo)(function () {
+      var previewClassName = "".concat(prefixCls, "-preview ").concat(previewOptions.className || '');
       return /*#__PURE__*/(0, _jsxRuntime.jsx)("div", {
         ref: previewRef,
         className: previewClassName,
@@ -244,11 +235,12 @@ function createMDEditor(options) {
           source: state.markdown || ''
         }))
       });
-    }, [previewClassName, previewOptions, state.markdown]);
+    }, [prefixCls, previewOptions, state.markdown]);
     var preview = (components === null || components === void 0 ? void 0 : components.preview) && (components === null || components === void 0 ? void 0 : components.preview(state.markdown || '', state, dispatch));
     if (preview && /*#__PURE__*/_react["default"].isValidElement(preview)) {
+      var customPreviewClassName = "".concat(prefixCls, "-preview ").concat(previewOptions.className || '');
       mdPreview = /*#__PURE__*/(0, _jsxRuntime.jsx)("div", {
-        className: previewClassName,
+        className: customPreviewClassName,
         ref: previewRef,
         onScroll: handlePreviewScroll,
         children: preview
@@ -267,7 +259,7 @@ function createMDEditor(options) {
         height: newHeight
       });
     };
-    var changeHandle = function changeHandle(evn) {
+    var changeHandle = (0, _react.useCallback)(function (evn) {
       onChange && onChange(evn.target.value, evn, state);
       if (textareaProps && textareaProps.onChange) {
         textareaProps.onChange(evn);
@@ -280,7 +272,7 @@ function createMDEditor(options) {
           length: evn.target.value.length
         }));
       }
-    };
+    }, [onChange, state, textareaProps, onStatistics]);
     return /*#__PURE__*/(0, _jsxRuntime.jsx)(_Context.EditorContext.Provider, {
       value: (0, _objectSpread2["default"])((0, _objectSpread2["default"])({}, state), {}, {
         dispatch: dispatch
diff --git a/node_modules/@uiw/react-md-editor/lib/commands/preview.js b/node_modules/@uiw/react-md-editor/lib/commands/preview.js
index 4e3e225..b7072d4 100644
--- a/node_modules/@uiw/react-md-editor/lib/commands/preview.js
+++ b/node_modules/@uiw/react-md-editor/lib/commands/preview.js
@@ -20,6 +20,8 @@ var codePreview = exports.codePreview = {
     width: "12",
     height: "12",
     viewBox: "0 0 520 520",
+    role: "img",
+    "aria-hidden": "true",
     children: [/*#__PURE__*/(0, _jsxRuntime.jsx)("polygon", {
       fill: "currentColor",
       points: "0 71.293 0 122 38.023 123 38.023 398 0 397 0 449.707 91.023 450.413 91.023 72.293"
@@ -28,9 +30,9 @@ var codePreview = exports.codePreview = {
       points: "148.023 72.293 520 71.293 520 122 200.023 124 200.023 397 520 396 520 449.707 148.023 450.413"
     })]
   }),
-  execute: function execute(state, api, dispatch, executeCommandState, shortcuts) {
-    api.textArea.focus();
-    if (shortcuts && dispatch && executeCommandState) {
+  execute: function execute(_state, api, dispatch, _executeCommandState, shortcuts) {
+    api.textArea && api.textArea.focus();
+    if (shortcuts && dispatch && _executeCommandState) {
       dispatch({
         preview: 'preview'
       });
@@ -50,6 +52,8 @@ var codeEdit = exports.codeEdit = {
     width: "12",
     height: "12",
     viewBox: "0 0 520 520",
+    role: "img",
+    "aria-hidden": "true",
     children: [/*#__PURE__*/(0, _jsxRuntime.jsx)("polygon", {
       fill: "currentColor",
       points: "0 71.293 0 122 319 122 319 397 0 397 0 449.707 372 449.413 372 71.293"
@@ -58,9 +62,9 @@ var codeEdit = exports.codeEdit = {
       points: "429 71.293 520 71.293 520 122 481 123 481 396 520 396 520 449.707 429 449.413"
     })]
   }),
-  execute: function execute(state, api, dispatch, executeCommandState, shortcuts) {
-    api.textArea.focus();
-    if (shortcuts && dispatch && executeCommandState) {
+  execute: function execute(_state, api, dispatch, _executeCommandState, shortcuts) {
+    api.textArea && api.textArea.focus();
+    if (shortcuts && dispatch && _executeCommandState) {
       dispatch({
         preview: 'edit'
       });
@@ -80,6 +84,8 @@ var codeLive = exports.codeLive = {
     width: "12",
     height: "12",
     viewBox: "0 0 520 520",
+    role: "img",
+    "aria-hidden": "true",
     children: [/*#__PURE__*/(0, _jsxRuntime.jsx)("polygon", {
       fill: "currentColor",
       points: "0 71.293 0 122 179 122 179 397 0 397 0 449.707 232 449.413 232 71.293"
@@ -88,9 +94,9 @@ var codeLive = exports.codeLive = {
       points: "289 71.293 520 71.293 520 122 341 123 341 396 520 396 520 449.707 289 449.413"
     })]
   }),
-  execute: function execute(state, api, dispatch, executeCommandState, shortcuts) {
-    api.textArea.focus();
-    if (shortcuts && dispatch && executeCommandState) {
+  execute: function execute(_state, api, dispatch, _executeCommandState, shortcuts) {
+    api.textArea && api.textArea.focus();
+    if (shortcuts && dispatch && _executeCommandState) {
       dispatch({
         preview: 'live'
       });
diff --git a/node_modules/@uiw/react-md-editor/src/Context.tsx b/node_modules/@uiw/react-md-editor/src/Context.tsx
index 97ca08f..ab905aa 100644
--- a/node_modules/@uiw/react-md-editor/src/Context.tsx
+++ b/node_modules/@uiw/react-md-editor/src/Context.tsx
@@ -26,13 +26,45 @@ export interface ContextStore {
   scrollTopPreview?: number;
   tabSize?: number;
   defaultTabEnable?: boolean;
-  [key: string]: any;
 }
 
-export type ExecuteCommandState = Pick<ContextStore, 'fullscreen' | 'preview' | 'highlightEnable'>;
+export interface ExecuteCommandState {
+  fullscreen?: boolean;
+  preview?: PreviewType;
+  highlightEnable?: boolean;
+}
+
+// SEC-CTX-04: Reducer whitelist — only known keys are merged into state
+const REDUCER_ALLOWED_KEYS = new Set([
+  'components', 'commands', 'extraCommands', 'markdown', 'preview',
+  'height', 'fullscreen', 'highlightEnable', 'autoFocus', 'autoFocusEnd',
+  'textarea', 'commandOrchestrator', 'textareaWarp', 'textareaPre',
+  'container', 'dispatch', 'barPopup', 'scrollTop', 'scrollTopPreview',
+  'tabSize', 'defaultTabEnable',
+]);
 
-export function reducer(state: ContextStore, action: ContextStore) {
-  return { ...state, ...action };
+export function reducer(state: ContextStore, action: ContextStore): ContextStore {
+  if (!action || typeof action !== 'object') return state;
+  const filtered: Partial<ContextStore> = {};
+  for (const key of Object.keys(action)) {
+    if (REDUCER_ALLOWED_KEYS.has(key)) {
+      (filtered as Record<string, unknown>)[key] = action[key as keyof ContextStore];
+    }
+  }
+  return { ...state, ...filtered };
 }
 
-export const EditorContext = React.createContext<ContextStore>({ markdown: '' });
+// SEC-CTX-05: Complete default values for ContextStore
+export const EditorContext = React.createContext<ContextStore>({
+  markdown: '',
+  preview: 'live',
+  fullscreen: false,
+  highlightEnable: true,
+  autoFocus: false,
+  autoFocusEnd: false,
+  barPopup: {},
+  scrollTop: 0,
+  scrollTopPreview: 0,
+  tabSize: 2,
+  defaultTabEnable: true,
+});
diff --git a/node_modules/@uiw/react-md-editor/src/Editor.factory.tsx b/node_modules/@uiw/react-md-editor/src/Editor.factory.tsx
index c9c9ee1..3ee6f57 100644
--- a/node_modules/@uiw/react-md-editor/src/Editor.factory.tsx
+++ b/node_modules/@uiw/react-md-editor/src/Editor.factory.tsx
@@ -1,4 +1,4 @@
-import React, { useEffect, useReducer, useMemo, useRef, useImperativeHandle } from 'react';
+import React, { useCallback, useEffect, useReducer, useMemo, useRef, useImperativeHandle } from 'react';
 import { ToolbarVisibility } from './components/Toolbar/';
 import DragBar from './components/DragBar/';
 import { getCommands, getExtraCommands, type ICommand, type TextState, TextAreaCommandOrchestrator } from './commands/';
@@ -6,10 +6,11 @@ import { reducer, EditorContext, type ContextStore } from './Context';
 import type { MDEditorProps } from './Types';
 
 function setGroupPopFalse(data: Record<string, boolean> = {}) {
+  const result: Record<string, boolean> = {};
   Object.keys(data).forEach((keyname) => {
-    data[keyname] = false;
+    result[keyname] = false;
   });
-  return data;
+  return result;
 }
 
 export interface RefMDEditor extends ContextStore {}
@@ -95,7 +96,7 @@ export function createMDEditor<
         stateInit.markdown = propsValue || '';
         stateInit.barPopup = {};
         if (dispatch) {
-          dispatch({ ...state, ...stateInit });
+          dispatch(stateInit);
         }
         // eslint-disable-next-line react-hooks/exhaustive-deps
       }, []);
@@ -112,68 +113,86 @@ export function createMDEditor<
         .join(' ')
         .trim();
 
-      useMemo(
-        () => propsValue !== state.markdown && dispatch({ markdown: propsValue || '' }),
-        [propsValue, state.markdown],
-      );
-      // eslint-disable-next-line react-hooks/exhaustive-deps
-      useMemo(() => previewType !== state.preview && dispatch({ preview: previewType }), [previewType]);
-      // eslint-disable-next-line react-hooks/exhaustive-deps
-      useMemo(() => tabSize !== state.tabSize && dispatch({ tabSize }), [tabSize]);
-      useMemo(
-        () => highlightEnable !== state.highlightEnable && dispatch({ highlightEnable }),
-        // eslint-disable-next-line react-hooks/exhaustive-deps
-        [highlightEnable],
-      );
-      // eslint-disable-next-line react-hooks/exhaustive-deps
-      useMemo(() => autoFocus !== state.autoFocus && dispatch({ autoFocus: autoFocus }), [autoFocus]);
-      useMemo(() => autoFocusEnd !== state.autoFocusEnd && dispatch({ autoFocusEnd: autoFocusEnd }), [autoFocusEnd]);
-      useMemo(
-        () => fullscreen !== state.fullscreen && dispatch({ fullscreen: fullscreen }),
-        // eslint-disable-next-line react-hooks/exhaustive-deps
-        [fullscreen],
-      );
-      // eslint-disable-next-line react-hooks/exhaustive-deps
-      useMemo(() => height !== state.height && dispatch({ height: height }), [height]);
-      useMemo(
-        () => height !== state.height && onHeightChange && onHeightChange(state.height, height, state),
-        [height, onHeightChange, state],
-      );
-      // eslint-disable-next-line react-hooks/exhaustive-deps
-      useMemo(() => commands !== state.commands && dispatch({ commands: cmds }), [props.commands]);
-      // eslint-disable-next-line react-hooks/exhaustive-deps
-      useMemo(
-        () => extraCommands !== state.extraCommands && dispatch({ extraCommands: extraCmds }),
-        [props.extraCommands],
-      );
+      useEffect(() => {
+        if (propsValue !== state.markdown) {
+          dispatch({ markdown: propsValue || '' });
+        }
+      }, [propsValue]);
+      useEffect(() => {
+        if (previewType !== state.preview) {
+          dispatch({ preview: previewType });
+        }
+      }, [previewType]);
+      useEffect(() => {
+        if (tabSize !== state.tabSize) {
+          dispatch({ tabSize });
+        }
+      }, [tabSize]);
+      useEffect(() => {
+        if (highlightEnable !== state.highlightEnable) {
+          dispatch({ highlightEnable });
+        }
+      }, [highlightEnable]);
+      useEffect(() => {
+        if (autoFocus !== state.autoFocus) {
+          dispatch({ autoFocus: autoFocus });
+        }
+      }, [autoFocus]);
+      useEffect(() => {
+        if (autoFocusEnd !== state.autoFocusEnd) {
+          dispatch({ autoFocusEnd: autoFocusEnd });
+        }
+      }, [autoFocusEnd]);
+      useEffect(() => {
+        if (fullscreen !== state.fullscreen) {
+          dispatch({ fullscreen: fullscreen });
+        }
+      }, [fullscreen]);
+      useEffect(() => {
+        if (height !== state.height) {
+          const oldHeight = state.height;
+          dispatch({ height: height });
+          if (onHeightChange) {
+            onHeightChange(oldHeight, height, state);
+          }
+        }
+      }, [height]);
+      useEffect(() => {
+        if (commands !== state.commands) {
+          dispatch({ commands: cmds });
+        }
+      }, [props.commands]);
+      useEffect(() => {
+        if (extraCommands !== state.extraCommands) {
+          dispatch({ extraCommands: extraCmds });
+        }
+      }, [props.extraCommands]);
 
       const textareaDomRef = useRef<HTMLDivElement>();
       const active = useRef<'text' | 'preview'>('preview');
-      const initScroll = useRef(false);
 
-      useMemo(() => {
+      useEffect(() => {
         textareaDomRef.current = state.textareaWarp;
-        if (state.textareaWarp) {
-          state.textareaWarp.addEventListener('mouseover', () => {
-            active.current = 'text';
-          });
-          state.textareaWarp.addEventListener('mouseleave', () => {
-            active.current = 'preview';
-          });
-        }
+        if (!state.textareaWarp) return;
+        const onMouseOver = () => { active.current = 'text'; };
+        const onMouseLeave = () => { active.current = 'preview'; };
+        state.textareaWarp.addEventListener('mouseover', onMouseOver);
+        state.textareaWarp.addEventListener('mouseleave', onMouseLeave);
+        return () => {
+          state.textareaWarp!.removeEventListener('mouseover', onMouseOver);
+          state.textareaWarp!.removeEventListener('mouseleave', onMouseLeave);
+        };
       }, [state.textareaWarp]);
 
       const handleScroll = (e: React.UIEvent<HTMLDivElement>, type: 'text' | 'preview') => {
         if (!enableScrollRef.current) return;
         const textareaDom = textareaDomRef.current;
-        const previewDom = previewRef.current ? previewRef.current : undefined;
-        if (!initScroll.current) {
-          active.current = type;
-          initScroll.current = true;
-        }
+        const previewDom = previewRef.current;
         if (textareaDom && previewDom) {
-          const scale =
-            (textareaDom.scrollHeight - textareaDom.offsetHeight) / (previewDom.scrollHeight - previewDom.offsetHeight);
+          const numerator = textareaDom.scrollHeight - textareaDom.offsetHeight;
+          const denominator = previewDom.scrollHeight - previewDom.offsetHeight;
+          if (denominator === 0 || numerator === 0) return;
+          const scale = numerator / denominator;
           if (e.target === textareaDom && active.current === 'text') {
             previewDom.scrollTop = textareaDom.scrollTop / scale;
           }
@@ -190,20 +209,23 @@ export function createMDEditor<
         }
       };
 
-      const previewClassName = `${prefixCls}-preview ${previewOptions.className || ''}`;
       const handlePreviewScroll = (e: React.UIEvent<HTMLDivElement, UIEvent>) => handleScroll(e, 'preview');
       let mdPreview = useMemo(
-        () => (
-          <div ref={previewRef} className={previewClassName}>
-            <PreviewComponent {...previewOptions} onScroll={handlePreviewScroll} source={state.markdown || ''} />
-          </div>
-        ),
-        [previewClassName, previewOptions, state.markdown],
+        () => {
+          const previewClassName = `${prefixCls}-preview ${previewOptions.className || ''}`;
+          return (
+            <div ref={previewRef} className={previewClassName}>
+              <PreviewComponent {...previewOptions} onScroll={handlePreviewScroll} source={state.markdown || ''} />
+            </div>
+          );
+        },
+        [prefixCls, previewOptions, state.markdown],
       );
       const preview = components?.preview && components?.preview(state.markdown || '', state, dispatch);
       if (preview && React.isValidElement(preview)) {
+        const customPreviewClassName = `${prefixCls}-preview ${previewOptions.className || ''}`;
         mdPreview = (
-          <div className={previewClassName} ref={previewRef} onScroll={handlePreviewScroll}>
+          <div className={customPreviewClassName} ref={previewRef} onScroll={handlePreviewScroll}>
             {preview}
           </div>
         );
@@ -213,7 +235,7 @@ export function createMDEditor<
       const containerClick = () => dispatch({ barPopup: { ...setGroupPopFalse(state.barPopup) } });
       const dragBarChange = (newHeight: number) => dispatch({ height: newHeight });
 
-      const changeHandle = (evn: React.ChangeEvent<HTMLTextAreaElement>) => {
+      const changeHandle = useCallback((evn: React.ChangeEvent<HTMLTextAreaElement>) => {
         onChange && onChange(evn.target.value, evn, state);
         if (textareaProps && textareaProps.onChange) {
           textareaProps.onChange(evn);
@@ -227,7 +249,7 @@ export function createMDEditor<
             length: evn.target.value.length,
           });
         }
-      };
+      }, [onChange, state, textareaProps, onStatistics]);
       return (
         <EditorContext.Provider value={{ ...state, dispatch }}>
           <div ref={container} className={cls} {...other} onClick={containerClick} style={containerStyle}>
diff --git a/node_modules/@uiw/react-md-editor/src/commands/group.tsx b/node_modules/@uiw/react-md-editor/src/commands/group.tsx
index b9ca7f4..f6b71e2 100644
--- a/node_modules/@uiw/react-md-editor/src/commands/group.tsx
+++ b/node_modules/@uiw/react-md-editor/src/commands/group.tsx
@@ -1,13 +1,23 @@
 import React from 'react';
 import { type ICommand, type ICommandChildCommands, type ICommandChildHandle } from './';
 
-export type GroupOptions = Omit<ICommand<string>, 'children'> & {
+export type GroupOptions = Omit<ICommand<string>, 'children' | 'execute' | 'icon'> & {
   children?: ICommandChildHandle['children'];
 };
 
 export const group = (arr: ICommandChildCommands['children'], options?: GroupOptions): ICommand<string> => {
-  let data = {
-    children: arr as any,
+  // SEC-G-03: Input validation — reject non-array, non-null values
+  if (arr != null && !Array.isArray(arr)) {
+    throw new TypeError('group(): arr must be an array or null/undefined');
+  }
+
+  // SEC-G-01: Validate and filter children instead of using 'as any'
+  const validatedChildren: Array<ICommand<string>> = Array.isArray(arr)
+    ? arr.filter((item): item is ICommand<string> => item != null && typeof item === 'object')
+    : [];
+
+  // SEC-G-04: Spread options first, then override protected properties (children, execute, icon)
+  let data: ICommand<string> = {
     icon: (
       <svg width="12" height="12" viewBox="0 0 520 520">
         <path
@@ -18,13 +28,20 @@ export const group = (arr: ICommandChildCommands['children'], options?: GroupOpt
     ),
     execute: () => {},
     ...options,
+    children: validatedChildren,
     keyCommand: 'group',
   };
-  if (Array.isArray(data.children)) {
-    data.children = data.children.map(({ ...item }: ICommand) => {
-      item.parent = data;
-      return { ...item };
-    });
-  }
+
+  // SEC-G-02: Lightweight parent marker to avoid circular reference
+  // SEC-G-05: Single spread for clean shallow clone
+  const parentMarker = { keyCommand: 'group', groupName: data.groupName } as ICommand<any>;
+  data = {
+    ...data,
+    children: validatedChildren.map((item: ICommand<string>) => ({
+      ...item,
+      parent: parentMarker,
+    })),
+  };
+
   return data;
 };
diff --git a/node_modules/@uiw/react-md-editor/src/commands/preview.tsx b/node_modules/@uiw/react-md-editor/src/commands/preview.tsx
index f163172..2dc44b2 100644
--- a/node_modules/@uiw/react-md-editor/src/commands/preview.tsx
+++ b/node_modules/@uiw/react-md-editor/src/commands/preview.tsx
@@ -9,7 +9,7 @@ export const codePreview: ICommand = {
   shortcuts: 'ctrlcmd+9',
   buttonProps: { 'aria-label': 'Preview code (ctrl + 9)', title: 'Preview code (ctrl + 9)' },
   icon: (
-    <svg width="12" height="12" viewBox="0 0 520 520">
+    <svg width="12" height="12" viewBox="0 0 520 520" role="img" aria-hidden="true">
       <polygon
         fill="currentColor"
         points="0 71.293 0 122 38.023 123 38.023 398 0 397 0 449.707 91.023 450.413 91.023 72.293"
@@ -21,14 +21,14 @@ export const codePreview: ICommand = {
     </svg>
   ),
   execute: (
-    state: TextState,
+    _state: TextState,
     api: TextAreaTextApi,
     dispatch?: React.Dispatch<ContextStore>,
-    executeCommandState?: ExecuteCommandState,
+    _executeCommandState?: ExecuteCommandState,
     shortcuts?: string[],
   ) => {
-    api.textArea.focus();
-    if (shortcuts && dispatch && executeCommandState) {
+    api.textArea?.focus();
+    if (shortcuts && dispatch && _executeCommandState) {
       dispatch({ preview: 'preview' });
     }
   },
@@ -41,7 +41,7 @@ export const codeEdit: ICommand = {
   shortcuts: 'ctrlcmd+7',
   buttonProps: { 'aria-label': 'Edit code (ctrl + 7)', title: 'Edit code (ctrl + 7)' },
   icon: (
-    <svg width="12" height="12" viewBox="0 0 520 520">
+    <svg width="12" height="12" viewBox="0 0 520 520" role="img" aria-hidden="true">
       <polygon fill="currentColor" points="0 71.293 0 122 319 122 319 397 0 397 0 449.707 372 449.413 372 71.293" />
       <polygon
         fill="currentColor"
@@ -50,14 +50,14 @@ export const codeEdit: ICommand = {
     </svg>
   ),
   execute: (
-    state: TextState,
+    _state: TextState,
     api: TextAreaTextApi,
     dispatch?: React.Dispatch<ContextStore>,
-    executeCommandState?: ExecuteCommandState,
+    _executeCommandState?: ExecuteCommandState,
     shortcuts?: string[],
   ) => {
-    api.textArea.focus();
-    if (shortcuts && dispatch && executeCommandState) {
+    api.textArea?.focus();
+    if (shortcuts && dispatch && _executeCommandState) {
       dispatch({ preview: 'edit' });
     }
   },
@@ -70,7 +70,7 @@ export const codeLive: ICommand = {
   shortcuts: 'ctrlcmd+8',
   buttonProps: { 'aria-label': 'Live code (ctrl + 8)', title: 'Live code (ctrl + 8)' },
   icon: (
-    <svg width="12" height="12" viewBox="0 0 520 520">
+    <svg width="12" height="12" viewBox="0 0 520 520" role="img" aria-hidden="true">
       <polygon fill="currentColor" points="0 71.293 0 122 179 122 179 397 0 397 0 449.707 232 449.413 232 71.293" />
       <polygon
         fill="currentColor"
@@ -79,14 +79,14 @@ export const codeLive: ICommand = {
     </svg>
   ),
   execute: (
-    state: TextState,
+    _state: TextState,
     api: TextAreaTextApi,
     dispatch?: React.Dispatch<ContextStore>,
-    executeCommandState?: ExecuteCommandState,
+    _executeCommandState?: ExecuteCommandState,
     shortcuts?: string[],
   ) => {
-    api.textArea.focus();
-    if (shortcuts && dispatch && executeCommandState) {
+    api.textArea?.focus();
+    if (shortcuts && dispatch && _executeCommandState) {
       dispatch({ preview: 'live' });
     }
   },
diff --git a/node_modules/@uiw/react-md-editor/src/commands/table.tsx b/node_modules/@uiw/react-md-editor/src/commands/table.tsx
index a0cc770..d46d53d 100644
--- a/node_modules/@uiw/react-md-editor/src/commands/table.tsx
+++ b/node_modules/@uiw/react-md-editor/src/commands/table.tsx
@@ -1,52 +1,124 @@
+// Font Awesome Free 6.4.2 by @fontawesome - https://fontawesome.com
 import React from 'react';
 import { ICommand, ExecuteState, TextAreaTextApi } from './';
-import { selectWord, executeCommand } from '../utils/markdownUtils';
+
+const TABLE_TEMPLATE = [
+  '',
+  '| Header | Header |',
+  '|--------|--------|',
+  '| Cell | Cell |',
+  '| Cell | Cell |',
+  '| Cell | Cell |',
+  '',
+].join('\n');
+
+/**
+ * Checks if a line is a Markdown table row (starts with '|')
+ */
+function isTableRow(line: string): boolean {
+  return line.trim().startsWith('|');
+}
+
+/**
+ * Checks if a line is a Markdown table separator (e.g. |---|---|)
+ */
+function isTableSeparator(line: string): boolean {
+  return /^\|[\s]*-{3,}[\s]*(\|[\s]*-{3,}[\s]*)*\|?$/.test(line.trim());
+}
+
+/**
+ * Finds the full table block range containing the given cursor position.
+ * Returns null if the cursor is not inside a valid Markdown table.
+ */
+function findTableBlock(text: string, position: number): { start: number; end: number } | null {
+  const lines = text.split('\n');
+  let charPos = 0;
+  let cursorLine = 0;
+
+  // Find which line the cursor is on
+  for (let i = 0; i < lines.length; i++) {
+    const lineEnd = charPos + lines[i].length;
+    if (position <= lineEnd) {
+      cursorLine = i;
+      break;
+    }
+    charPos = lineEnd + 1;
+    if (i === lines.length - 1) cursorLine = i;
+  }
+
+  // Check if cursor line is a table row or separator
+  if (!isTableRow(lines[cursorLine]) && !isTableSeparator(lines[cursorLine])) {
+    return null;
+  }
+
+  // Expand upward to find table start
+  let startLine = cursorLine;
+  while (startLine > 0 && (isTableRow(lines[startLine - 1]) || isTableSeparator(lines[startLine - 1]))) {
+    startLine--;
+  }
+
+  // Expand downward to find table end
+  let endLine = cursorLine;
+  while (endLine < lines.length - 1 && (isTableRow(lines[endLine + 1]) || isTableSeparator(lines[endLine + 1]))) {
+    endLine++;
+  }
+
+  // Validate: block must contain a separator line to be a real table
+  let hasSeparator = false;
+  for (let i = startLine; i <= endLine; i++) {
+    if (isTableSeparator(lines[i])) {
+      hasSeparator = true;
+      break;
+    }
+  }
+  if (!hasSeparator) return null;
+
+  // Convert line indices to character positions
+  let start = 0;
+  for (let i = 0; i < startLine; i++) {
+    start += lines[i].length + 1;
+  }
+
+  let end = start;
+  for (let i = startLine; i <= endLine; i++) {
+    end += lines[i].length + 1;
+  }
+  end = Math.min(end, text.length);
+
+  return { start, end };
+}
 
 export const table: ICommand = {
   name: 'table',
   keyCommand: 'table',
-  prefix: '\n| Header | Header |\n|--------|--------|\n| Cell | Cell |\n| Cell | Cell |\n| Cell | Cell |\n\n',
+  prefix: TABLE_TEMPLATE,
   suffix: '',
   buttonProps: { 'aria-label': 'Add table', title: 'Add table' },
   icon: (
-    <svg role="img" width="12" height="12" viewBox="0 0 512 512">
+    <svg role="img" width="12" height="12" viewBox="0 0 512 512" aria-hidden="true">
+      <title>Table</title>
       <path
         fill="currentColor"
         d="M64 256V160H224v96H64zm0 64H224v96H64V320zm224 96V320H448v96H288zM448 256H288V160H448v96zM64 32C28.7 32 0 60.7 0 96V416c0 35.3 28.7 64 64 64H448c35.3 0 64-28.7 64-64V96c0-35.3-28.7-64-64-64H64z"
-        //Font Awesome Free 6.4.2 by @fontawesome - https://fontawesome.com
       />
     </svg>
   ),
   execute: (state: ExecuteState, api: TextAreaTextApi) => {
-    const newSelectionRange = selectWord({
-      text: state.text,
-      selection: state.selection,
-      prefix: state.command.prefix!,
-      suffix: state.command.suffix,
-    });
-    let state1 = api.setSelectionRange(newSelectionRange);
-    if (
-      state1.selectedText.length >= state.command.prefix!.length + state.command.suffix!.length &&
-      state1.selectedText.startsWith(state.command.prefix!)
-    ) {
-      // Remove
-      executeCommand({
-        api,
-        selectedText: state1.selectedText,
-        selection: state.selection,
-        prefix: state.command.prefix!,
-        suffix: state.command.suffix,
-      });
+    // P2: Null guard — abort if prefix is undefined
+    const prefix = state.command.prefix;
+    if (!prefix) return;
+
+    // P1: Use line-level table structure detection instead of startsWith(prefix)
+    const tableBlock = findTableBlock(state.text, state.selection.start);
+
+    if (tableBlock) {
+      // Remove: select the full table block and delete it
+      api.setSelectionRange(tableBlock);
+      api.replaceSelection('');
     } else {
-      // Add
-      state1 = api.setSelectionRange({ start: state.selection.start, end: state.selection.start });
-      executeCommand({
-        api,
-        selectedText: state1.selectedText,
-        selection: state.selection,
-        prefix: state.command.prefix!,
-        suffix: state.command.suffix,
-      });
+      // Insert: place template at cursor position
+      api.setSelectionRange({ start: state.selection.start, end: state.selection.start });
+      api.replaceSelection(prefix);
     }
   },
 };
diff --git a/node_modules/@uiw/react-md-editor/src/commands/title1.tsx b/node_modules/@uiw/react-md-editor/src/commands/title1.tsx
index 09ff595..cd60751 100644
--- a/node_modules/@uiw/react-md-editor/src/commands/title1.tsx
+++ b/node_modules/@uiw/react-md-editor/src/commands/title1.tsx
@@ -11,13 +11,13 @@ export const heading1: ICommand = {
   buttonProps: { 'aria-label': 'Insert Heading 1 (ctrl + 1)', title: 'Insert Heading 1 (ctrl + 1)' },
   icon: <div style={{ fontSize: 18, textAlign: 'left' }}>Heading 1</div>,
   execute: (state: ExecuteState, api: TextAreaTextApi) => {
-    headingExecute({ state, api, prefix: state.command.prefix!, suffix: state.command.suffix });
+    headingExecute({ state, api, prefix: state.command.prefix ?? '# ', suffix: state.command.suffix ?? '' });
   },
 };
 
 /**
- * @deprecated Use `heading1` instead.
- * This command is now deprecated and will be removed in future versions.
- * Use `title1` for inserting Heading 1.
+ * @deprecated Since v4.0.0. Use `heading1` instead.
+ * Scheduled for removal in v5.0.0.
+ * @see heading1
  */
 export const title1: ICommand = heading1;
diff --git a/node_modules/@uiw/react-md-editor/src/commands/title2.tsx b/node_modules/@uiw/react-md-editor/src/commands/title2.tsx
index 4ab3753..14ab295 100644
--- a/node_modules/@uiw/react-md-editor/src/commands/title2.tsx
+++ b/node_modules/@uiw/react-md-editor/src/commands/title2.tsx
@@ -11,13 +11,13 @@ export const heading2: ICommand = {
   buttonProps: { 'aria-label': 'Insert Heading 2 (ctrl + 2)', title: 'Insert Heading 2 (ctrl + 2)' },
   icon: <div style={{ fontSize: 16, textAlign: 'left' }}>Heading 2</div>,
   execute: (state: ExecuteState, api: TextAreaTextApi) => {
-    headingExecute({ state, api, prefix: state.command.prefix!, suffix: state.command.suffix });
+    headingExecute({ state, api, prefix: state.command.prefix ?? '## ', suffix: state.command.suffix ?? '' });
   },
 };
 
 /**
- * @deprecated Use `heading2` instead.
- * This command is now deprecated and will be removed in future versions.
- * Use `title2` for inserting Heading 2.
+ * @deprecated Since v4.0.0. Use `heading2` instead.
+ * Scheduled for removal in v5.0.0.
+ * @see heading2
  */
 export const title2: ICommand = heading2;
diff --git a/node_modules/@uiw/react-md-editor/src/commands/title3.tsx b/node_modules/@uiw/react-md-editor/src/commands/title3.tsx
index bde067d..fa3bfb8 100644
--- a/node_modules/@uiw/react-md-editor/src/commands/title3.tsx
+++ b/node_modules/@uiw/react-md-editor/src/commands/title3.tsx
@@ -11,13 +11,13 @@ export const heading3: ICommand = {
   buttonProps: { 'aria-label': 'Insert Heading 3 (ctrl + 3)', title: 'Insert Heading 3 (ctrl + 3)' },
   icon: <div style={{ fontSize: 15, textAlign: 'left' }}>Heading 3</div>,
   execute: (state: ExecuteState, api: TextAreaTextApi) => {
-    headingExecute({ state, api, prefix: state.command.prefix!, suffix: state.command.suffix });
+    headingExecute({ state, api, prefix: state.command.prefix ?? '### ', suffix: state.command.suffix ?? '' });
   },
 };
 
 /**
- * @deprecated Use `heading3` instead.
- * This command is now deprecated and will be removed in future versions.
- * Use `title3` for inserting Heading 3.
+ * @deprecated Since v4.0.0. Use `heading3` instead.
+ * Scheduled for removal in v5.0.0.
+ * @see heading3
  */
 export const title3: ICommand = heading3;
diff --git a/node_modules/@uiw/react-md-editor/src/commands/title4.tsx b/node_modules/@uiw/react-md-editor/src/commands/title4.tsx
index 219f84e..0915a95 100644
--- a/node_modules/@uiw/react-md-editor/src/commands/title4.tsx
+++ b/node_modules/@uiw/react-md-editor/src/commands/title4.tsx
@@ -11,13 +11,13 @@ export const heading4: ICommand = {
   buttonProps: { 'aria-label': 'Insert Heading 4 (ctrl + 4)', title: 'Insert Heading 4 (ctrl + 4)' },
   icon: <div style={{ fontSize: 14, textAlign: 'left' }}>Heading 4</div>,
   execute: (state: ExecuteState, api: TextAreaTextApi) => {
-    headingExecute({ state, api, prefix: state.command.prefix!, suffix: state.command.suffix });
+    headingExecute({ state, api, prefix: state.command.prefix ?? '#### ', suffix: state.command.suffix ?? '' });
   },
 };
 
 /**
- * @deprecated Use `heading4` instead.
- * This command is now deprecated and will be removed in future versions.
- * Use `title4` for inserting Heading 4.
+ * @deprecated Since v4.0.0. Use `heading4` instead.
+ * Scheduled for removal in v5.0.0.
+ * @see heading4
  */
 export const title4: ICommand = heading4;
diff --git a/node_modules/@uiw/react-md-editor/src/commands/title5.tsx b/node_modules/@uiw/react-md-editor/src/commands/title5.tsx
index 89347df..f2ca58c 100644
--- a/node_modules/@uiw/react-md-editor/src/commands/title5.tsx
+++ b/node_modules/@uiw/react-md-editor/src/commands/title5.tsx
@@ -11,13 +11,13 @@ export const heading5: ICommand = {
   buttonProps: { 'aria-label': 'Insert Heading 5 (ctrl + 5)', title: 'Insert Heading 5 (ctrl + 5)' },
   icon: <div style={{ fontSize: 12, textAlign: 'left' }}>Heading 5</div>,
   execute: (state: ExecuteState, api: TextAreaTextApi) => {
-    headingExecute({ state, api, prefix: state.command.prefix!, suffix: state.command.suffix });
+    headingExecute({ state, api, prefix: state.command.prefix ?? '##### ', suffix: state.command.suffix ?? '' });
   },
 };
 
 /**
- * @deprecated Use `heading5` instead.
- * This command is now deprecated and will be removed in future versions.
- * Use `title5` for inserting Heading 5.
+ * @deprecated Since v4.0.0. Use `heading5` instead.
+ * Scheduled for removal in v5.0.0.
+ * @see heading5
  */
 export const title5: ICommand = heading5;
diff --git a/node_modules/@uiw/react-md-editor/src/commands/title6.tsx b/node_modules/@uiw/react-md-editor/src/commands/title6.tsx
index d959038..0630d3f 100644
--- a/node_modules/@uiw/react-md-editor/src/commands/title6.tsx
+++ b/node_modules/@uiw/react-md-editor/src/commands/title6.tsx
@@ -11,13 +11,13 @@ export const heading6: ICommand = {
   buttonProps: { 'aria-label': 'Insert Heading 6 (ctrl + 6)', title: 'Insert Heading 6 (ctrl + 6)' },
   icon: <div style={{ fontSize: 12, textAlign: 'left' }}>Heading 6</div>,
   execute: (state: ExecuteState, api: TextAreaTextApi) => {
-    headingExecute({ state, api, prefix: state.command.prefix!, suffix: state.command.suffix });
+    headingExecute({ state, api, prefix: state.command.prefix ?? '###### ', suffix: state.command.suffix ?? '' });
   },
 };
 
 /**
- * @deprecated Use `heading6` instead.
- * This command is now deprecated and will be removed in future versions.
- * Use `title6` for inserting Heading 6.
+ * @deprecated Since v4.0.0. Use `heading6` instead.
+ * Scheduled for removal in v5.0.0.
+ * @see heading6
  */
 export const title6: ICommand = heading6;
